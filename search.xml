<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>LeetCode 1470. Shuffle the Array</title>
    <url>/2026/02/14/python/LeetCode-1470-Shuffle-the-Array/</url>
    <content><![CDATA[题目描述给你一个数组 nums，数组中有 2n 个元素，按 [x1, x2, ..., xn, y1, y2, ..., yn] 的格式排列。请你将数组按 [x1, y1, x2, y2, ..., xn, yn] 格式重新排列，返回重排后的数组。
示例 1：
输入：nums = [2,5,1,3,4,7], n = 3输出：[2,3,5,4,1,7]解释：x1=2, x2=5, x3=1, y1=3, y2=4, y3=7，重排后为 [2,3,5,4,1,7]

示例 2：
输入：nums = [1,2,3,4,4,3,2,1], n = 4输出：[1,4,2,3,3,2,4,1]

解题思路题目要求将数组从”前半部分 x + 后半部分 y”的排列方式转换为”交叉排列”的方式。换句话说，原数组的前 n 个元素是 x 组，后 n 个元素是 y 组，我们需要逐一交叉取出。
分析步骤：

数组长度为 2n，前 n 个元素 nums[0] 到 nums[n-1] 对应 x1 到 xn。
后 n 个元素 nums[n] 到 nums[2n-1] 对应 y1 到 yn。
目标顺序为：x1, y1, x2, y2, ..., xn, yn —— 即每次取一个 x，再取一个对应的 y。

算法通过循环实现：

遍历索引 i 从 0 到 n-1；
每次迭代中，先将 nums[i]（即 x_{i+1}）追加到结果列表；
再将 nums[n + i]（即 y_{i+1}）追加到结果列表；
循环结束后，结果列表恰好包含 2n 个元素，且顺序符合要求。

这种逐个追加的方式直观易懂，每个元素被访问恰好一次，结果列表的顺序自然形成交叉排列。
复杂度分析
时间复杂度： O(n)，其中 n 为参数中给定的 n（数组长度的一半）。循环执行 n 次，每次进行两次 append 操作，总共 2n 次操作，与数组长度成正比。
空间复杂度： O(n)，需要一个长度为 2n 的结果列表 res 来存储重排后的数组（不计返回值）。

代码from typing import Listclass Solution:    def shuffle(self, nums: List[int], n: int) -&gt; List[int]:        res = []        for i in range(n):            res.append(nums[i])            res.append(nums[n + i])        return res
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1929. Concatenation of Array</title>
    <url>/2026/02/14/python/LeetCode-1929-Concatenation-of-Array/</url>
    <content><![CDATA[题目描述给你一个长度为 n 的整数数组 nums。请你构建一个长度为 2n 的答案数组 ans，ans 由两个 nums 数组串联形成。形式化地，ans[i] == nums[i] 且 ans[i + n] == nums[i]（0 &lt;= i &lt; n）。返回数组 ans。
示例 1：
输入：nums = [1,2,1]输出：[1,2,1,1,2,1]

示例 2：
输入：nums = [1,3,2,1]输出：[1,3,2,1,1,3,2,1]

解题思路本题要求将原数组与自己拼接一次，核心是构造一个长度为 2n 的新数组，其中前 n 个元素与原数组完全相同，后 n 个元素也是原数组的副本。
在 Python 中，列表的 + 运算符本身就是序列拼接操作，它会创建一个新的列表对象，其中包含左侧列表的全部元素，随后紧接右侧列表的全部元素。这与题目要求的”串联”语义完全一致。
分析步骤：

nums 是一个包含 n 个元素的整数列表。
表达式 nums + nums 将 nums 与自身拼接，结果是一个长度为 2n 的新列表。
对于任意索引 i（0 &lt;= i &lt; n），ans[i] 等于 nums[i]，ans[i + n] 也等于 nums[i]，满足题目要求。

这种直接拼接的方式无需显式循环、无需逐个拷贝元素，代码极简且高效，充分利用了 Python 内置列表拼接的底层优化（底层 C 实现会一次性分配好结果列表所需的内存空间并批量拷贝指针）。
复杂度分析
时间复杂度： O(n)，其中 n 为数组长度。列表拼接操作需要将两个列表中的所有元素拷贝到新列表中，每个元素访问一次。
空间复杂度： O(n)，需要创建一个长度为 2n 的新列表作为返回值，不计算输入占用的空间。

代码from typing import Listdef getConcatenation(nums: List[int]) -&gt; List[int]:    return nums + nums
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 485. Max Consecutive Ones</title>
    <url>/2026/02/14/python/LeetCode-485-Max-Consecutive-Ones/</url>
    <content><![CDATA[题目描述给定一个二进制数组 nums，计算其中最大连续 1 的个数。
示例 1：
输入：nums = [1,1,0,1,1,1]输出：3解释：开头的两位和末尾的三位都是连续 1，所以最大连续 1 的个数是 3。

示例 2：
输入：nums = [1,0,1,1,0,1]输出：2

解题思路本题要求找出二进制数组中最长的一段连续 1 的长度。核心思想是遍历数组并维护两个变量：当前连续 1 的长度（pre_sum）和全局最大连续 1 的长度（max_sum）。
算法分析步骤：

初始化 pre_sum = 0（当前连续 1 的长度）和 max_sum = 0（全局最大值）。
遍历数组中的每个元素 nums[i]：
尝试将当前元素累加到 pre_sum 上，计算结果 res = pre_sum + nums[i]。
关键判断： 如果 res == pre_sum，说明 nums[i] == 0（累加后值不变，即遇到 0）。此时：
用当前的 pre_sum（即上一个连续 1 段的长度）更新 max_sum。
将 pre_sum 重置为 0，开始下一段计数。


否则（res &gt; pre_sum），说明 nums[i] == 1：
将 pre_sum 加 1，继续累加当前连续 1 的长度。




遍历结束后，最后一段连续 1 的长度可能还未与 max_sum 比较（如果数组以 1 结尾），因此返回 max(max_sum, pre_sum)。

为什么这个判断方式有效？ 因为二进制数组中元素只有 0 和 1。pre_sum + 1 &gt; pre_sum（遇到 1），而 pre_sum + 0 == pre_sum（遇到 0）。利用累加后是否变化来区分遇 0 和遇 1，同时用 pre_sum 记录当前连续 1 的计数。遇到 0 时结算一段，重置计数器；遇到 1 时继续累加。
复杂度分析
时间复杂度： O(n)，其中 n 为数组长度。只需要一次遍历即可完成计算。
空间复杂度： O(1)，只使用了常数个额外变量（pre_sum、max_sum、res），不随输入规模增长。

代码from typing import Listclass Solution:    def findMaxConsecutiveOnes(self, nums: List[int]) -&gt; int:        pre_sum = 0        max_sum = 0        for i in range(len(nums)):            res = pre_sum + nums[i]            if res == pre_sum:                max_sum = max(max_sum, res)                pre_sum = 0            else:                pre_sum += nums[i]        return max(max_sum, pre_sum)
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>LCR 061. 查找和最小的 K 对数字</title>
    <url>/2026/02/21/java/LCR-061-K-Smallest-Pairs/</url>
    <content><![CDATA[题目描述给定两个以升序排列的整数数组 nums1 和 nums2，以及一个整数 k。定义一对值 (u,v)，其中第一个元素来自 nums1，第二个元素来自 nums2。请找到和最小的 k 个数对 (u1,v1), (u2,v2) ... (uk,vk)。
例如：nums1 = [1,7,11]，nums2 = [2,4,6]，k = 3，输出 [[1,2],[1,4],[1,6]]。
解释：和最小的三对依次是 (1,2) 和&#x3D;3，(1,4) 和&#x3D;5，(1,6) 和&#x3D;7。
解题思路暴力法枚举所有 m * n 个数对，排序后取前 k 个。时间复杂度 O(mn log(mn))。当数组较大时效率很低。
最小堆优化（O(k log k)）核心思想：利用两个数组升序排列的性质，使用最小堆来”多路归并”。
关键观察对于固定 nums1[i]，它与 nums2 中元素配对的和是递增的：
(i,0): nums1[i] + nums2[0]  （最小）(i,1): nums1[i] + nums2[1](i,2): nums1[i] + nums2[2]...

这类似于合并 k 个有序链表。
算法步骤
初始化堆：将每行的最小值放入最小堆，即对于 i = 0..min(nums1.length, k)-1，将 (nums1[i] + nums2[0], i, 0) 插入堆中。堆按”和”从小到大排序。

贪心选取：循环 k 次（或直到堆空）：

弹出堆顶元素 (sum, i, j)，这就是当前最小的数对。
将 (nums1[i], nums2[j]) 加入结果集。
如果 j + 1 &lt; nums2.length，将下一列的元素 (nums1[i] + nums2[j+1], i, j+1) 插入堆中（按行指针后移）。


返回结果集。


示例说明以 nums1 = [1,7,11]，nums2 = [2,4,6]，k = 3 为例：
初始化堆：插入 (1+2&#x3D;3, i&#x3D;0, j&#x3D;0), (7+2&#x3D;9, i&#x3D;1, j&#x3D;0), (11+2&#x3D;13, i&#x3D;2, j&#x3D;0) → 堆：[3, 9, 13]

第 1 轮：弹出 (3,0,0)，加入 [1,2]；插入 (1+4&#x3D;5, 0, 1) → 堆：[5, 9, 13]
第 2 轮：弹出 (5,0,1)，加入 [1,4]；插入 (1+6&#x3D;7, 0, 2) → 堆：[7, 9, 13]
第 3 轮：弹出 (7,0,2)，加入 [1,6]。k 轮完成。
结果：[[1,2],[1,4],[1,6]]

为什么高效？对于一个加法表，我们只需要维护最多 min(m, k) 行的最小值。每次弹出一个元素，只向堆中插入该行的下一个元素。总的时间复杂度为 O(k log min(m, k))，远优于 O(mn log(mn))。
复杂度分析
时间复杂度：O(k log min(m, k))，其中 m &#x3D; nums1.length。每次堆操作 O(log m)，共 k 次。
空间复杂度：O(min(m, k))，堆中最多存储 min(m, k) 个元素。

代码package lcr.J061;import datastructure.MyMinHeap;import java.util.*;class Node implements Comparable&lt;Node&gt; &#123;    int sum; int i; int j;    public Node(int sum, int i, int j) &#123; this.sum = sum; this.i = i; this.j = j; &#125;    @Override    public int compareTo(Node other) &#123; return Integer.compare(this.sum, other.sum); &#125;&#125;public class J061 &#123;    public List&lt;List&lt;Integer&gt;&gt; kSmallestPairs(int[] nums1, int[] nums2, int k) &#123;        List&lt;List&lt;Integer&gt;&gt; result = new ArrayList&lt;&gt;();        int heapCap = Math.min(nums1.length, k);        MyMinHeap&lt;Node&gt; minHeap = new MyMinHeap&lt;&gt;(heapCap);        for (int i = 0; i &lt; heapCap; i++) &#123;            minHeap.insert(new Node(nums1[i] + nums2[0], i, 0));        &#125;        while (k &gt; 0 &amp;&amp; !minHeap.isEmpty()) &#123;            Node cur = minHeap.extractMin();            List&lt;Integer&gt; pair = new ArrayList&lt;&gt;();            pair.add(nums1[cur.i]);            pair.add(nums2[cur.j]);            result.add(pair);            k--;            if (cur.j + 1 &lt; nums2.length) &#123;                minHeap.insert(new Node(nums1[cur.i] + nums2[cur.j + 1], cur.i, cur.j + 1));            &#125;        &#125;        return result;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>堆</tag>
        <tag>最小堆</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1. Two Sum</title>
    <url>/2026/02/28/java/LeetCode-1-Two-Sum/</url>
    <content><![CDATA[题目描述给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。
解题思路本题要求在一个数组中找出两个和为 target 的数，并返回它们的下标。最直观的做法是双重循环遍历，时间复杂度为 O(n²)，但这不是最优解。
更高效的做法是使用哈希表。核心思路是：遍历数组，对于每个元素 nums[i]，我们计算出它需要的配对值 target - nums[i]，然后在哈希表中查找该配对值是否已经出现过。如果出现过，说明我们找到了答案，直接返回两个下标即可；如果没有出现，则将当前元素的值和下标存入哈希表，供后面的元素查找。
这样做的好处是：

每个元素只需要遍历一次，时间复杂度为 O(n)
通过哈希表的 O(1) 查找，快速判断配对值是否存在
一次遍历即可完成查找和存储，简洁高效

复杂度分析
时间复杂度：O(n)，其中 n 是数组长度。只需要遍历一次数组，每次查找和插入哈希表都是 O(1)。
空间复杂度：O(n)，哈希表中最多存储 n 个元素。

代码package code.J1;import java.util.HashMap;import java.util.Map;public class J1 &#123;    public int[] twoSum(int[] nums, int target) &#123;        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();        for (int i = 0; i &lt; nums.length; i++) &#123;            if (map.containsKey(target - nums[i])) &#123;                return new int[]&#123;map.get(target - nums[i]), i&#125;;            &#125;            map.put(nums[i], i);        &#125;        return null;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>哈希表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1019. Next Greater Node In Linked List</title>
    <url>/2026/02/28/java/LeetCode-1019-Next-Greater-Node-In-Linked-List/</url>
    <content><![CDATA[题目描述给定一个长度为 n 的链表，对于列表中的每个节点，查找下一个更大节点的值。返回一个整数数组 answer，其中 answer[i] 是第 i 个节点下一个更大节点的值。如果第 i 个节点没有下一个更大节点的值，设置 answer[i] = 0。
例如：链表 [2,1,5] → 输出 [5,5,0]

节点 2：下一个更大的是 5
节点 1：下一个更大的是 5
节点 5：后面没有更大的，为 0

再如：链表 [2,7,4,3,5] → 输出 [7,0,5,5,0]
解题思路本题是”下一个更大元素”问题的变种，只不过数据结构从数组变成了链表。核心思路依然是单调栈。
整体流程
链表转数组：首先遍历链表，将所有节点的值存入一个 ArrayList 中，这样可以方便地通过下标随机访问。同时记录元素总数 n。

单调递减栈求解：使用一个单调递减栈（存储下标），从左到右遍历数组：

当栈不为空，且当前元素 nums.get(i) 大于栈顶下标对应元素时：
弹出栈顶下标 index，意味着节点 index 找到了下一个更大的值。
设置 answer[index] = nums.get(i)。


将当前下标 i 入栈。


遍历结束后，栈中剩余的下标对应的元素没有找到下一个更大的值，answer 数组中对应位置保持默认值 0。


为什么用单调栈？对于类似”找下一个更大&#x2F;更小的元素”的问题，单调栈可以将暴力的 O(n²) 降低到 O(n)。核心思想是：

当遇到一个更大的元素时，栈中所有比它小的元素都在”等待”被匹配，一旦遇到更大的，就逐个结算。
栈的单调性保证了从未被弹出的元素不会被遗漏。

与 LeetCode 739 “每日温度” 的对比本题与第 739 题几乎完全一致，唯一区别是数据来源是链表而非数组，以及”下一个更大”判定的对象是数值本身而非下标差值。解法思路完全相同：链表转数组 + 单调栈。
复杂度分析
时间复杂度：O(n)，其中 n 是链表长度。链表遍历一次，单调栈处理每个元素入栈出栈各一次，均为 O(n)。
空间复杂度：O(n)，需要存储数组形式的链表节点值以及单调栈，均为 O(n) 级别。

代码package code.J1019;import datastructure.ListNode;import java.util.ArrayDeque;import java.util.ArrayList;import java.util.Deque;import java.util.List;public class J1019 &#123;    public int[] nextLargerNodes(ListNode head) &#123;        List&lt;Integer&gt; nums = new ArrayList&lt;&gt;();        ListNode curr = head;        while (curr != null) &#123;            nums.add(curr.val);            curr = curr.next;        &#125;        int n = nums.size();        int[] answer = new int[n];        Deque&lt;Integer&gt; stack = new ArrayDeque&lt;&gt;();        for (int i = 0; i &lt; n; i++) &#123;            while (!stack.isEmpty() &amp;&amp; nums.get(i) &gt; nums.get(stack.peek())) &#123;                int index = stack.pop();                answer[index] = nums.get(i);            &#125;            stack.push(i);        &#125;        return answer;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>单调栈</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1046. Last Stone Weight</title>
    <url>/2026/02/21/java/LeetCode-1046-Last-Stone-Weight/</url>
    <content><![CDATA[题目描述有一堆石头，每块石头的重量都是正整数。
每一回合，从中选出两块最重的石头，然后将它们一起粉碎。假设两块石头的重量分别为 x 和 y，且 x &lt;= y：

如果 x == y，那么两块石头都会被完全粉碎；
如果 x != y，那么重量为 x 的石头将会完全粉碎，而重量为 y 的石头新重量为 y - x。

最后，最多只会剩下一块石头。返回此石头的重量。如果没有石头剩下，就返回 0。
例如：stones = [2,7,4,1,8,1]

第一轮：选出 8 和 7，剩下 8-7&#x3D;1，数组变为 [2,4,1,1,1]
第二轮：选出 4 和 2，剩下 4-2&#x3D;2，数组变为 [2,1,1,1]
第三轮：选出 2 和 1，剩下 2-1&#x3D;1，数组变为 [1,1,1]
第四轮：选出 1 和 1，完全粉碎，数组变为 [1]
返回 1

解题思路这道题完美适合使用最大堆（优先队列）来求解。因为每一轮都需要快速获取最大的两个元素，并在可能的情况下将差值插回。
大顶堆解法
将所有石头重量插入最大堆中。
当堆中元素数量 &gt; 1 时，循环执行：
取出最大的元素 max1（堆顶）。
再取出次大的元素 max2。
如果 max1 != max2，将差值 abs(max1 - max2) 插回堆中。
如果 max1 == max2，两块石头都销毁，无需插回。


循环结束后：
如果堆为空，返回 0；
否则返回堆中仅剩的一个元素。




注：代码中使用了自定义的 MyMaxHeap 数据结构，其功能等价于 java.util.PriorityQueue 配合 Collections.reverseOrder()（或者直接用负数存入小顶堆来模拟大顶堆）。

为什么用最大堆？每一轮需要获取”最大的两个数”，最大堆可以在 O(log n) 时间内完成取出最大元素和插入新元素的操作。如果每次都对数组排序或扫描查找最大元素，时间复杂度会达到 O(n² log n) 或 O(n²)，在大数据量下不可接受。
复杂度分析
时间复杂度：O(n log n)，其中 n 是石头数量。每轮操作（两次取出 + 一次插入）需要 O(log n)，最多进行 n-1 轮。
空间复杂度：O(n)，最大堆需要存储所有石头重量。

代码package code.J1046;import datastructure.MyMaxHeap;public class J046 &#123;    public int lastStoneWeight(int[] stones) &#123;        MyMaxHeap&lt;Integer&gt; myMaxHeap = new MyMaxHeap&lt;&gt;(stones.length);        for (int stone : stones) &#123;            myMaxHeap.insert(stone);        &#125;        while (myMaxHeap.size() &gt; 1) &#123;            int max1 = myMaxHeap.extractMax();            int max2 = myMaxHeap.extractMax();            if (max1 != max2) &#123;                myMaxHeap.insert(Math.abs(max1 - max2));            &#125;        &#125;        if (myMaxHeap.isEmpty()) return 0;        return myMaxHeap.extractMax();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>堆</tag>
        <tag>Java</tag>
        <tag>最大堆</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1365. How Many Numbers Are Smaller Than the Current Number</title>
    <url>/2026/02/14/java/LeetCode-1365-How-Many-Numbers-Are-Smaller-Than-the-Current-Number/</url>
    <content><![CDATA[题目描述给你一个数组 nums，对于其中每个元素 nums[i]，请你统计数组中比它小的所有数字的数目。以数组形式返回答案。
例如：nums = [8,1,2,2,3]，输出 [4,0,1,1,3]。
解释：对于 nums[0]=8，存在四个比它小的数字（1,2,2,3）；对于 nums[1]=1，没有比它小的数字；对于 nums[2]=2，存在一个比它小的数字（1）；以此类推。
解题思路暴力法最直接的想法是对于每个元素，遍历整个数组统计比它小的元素个数，时间复杂度 O(n²)。当数据量较大时效率很低。
排序 + 哈希表（O(n log n)）本题的关键观察是：当我们把数组排序后，每个元素在排序数组中的下标，恰好就等于比它小的元素个数（因为排序后前面全是小于等于它的元素）。
具体步骤：

克隆并排序：将原数组 nums 克隆到 nums2，然后对 nums2 进行排序。
建立映射：遍历排序后的数组，建立”元素值 → 最小下标”的映射。注意处理重复元素——对于重复元素，应取第一次出现的位置（即最小的下标），因为比它小的元素个数应该是排序后第一个该值出现的下标。
使用 map.put(nums2[i], Math.min(num, i)) 确保重复元素取最小下标。


生成结果：遍历原数组，对每个元素 nums[i]，直接从 map 中取出比它小的元素个数即可。

示例说明以 nums = [8,1,2,2,3] 为例：

排序后 nums2 = [1,2,2,3,8]
建立映射：1→0, 2→1, 3→3, 8→4
遍历原数组：8→4, 1→0, 2→1, 2→1, 3→3，结果 [4,0,1,1,3]。

复杂度分析
时间复杂度：O(n log n)，排序 O(n log n)，两次遍历各 O(n)。
空间复杂度：O(n)，需要克隆数组和哈希表的额外空间。

代码package code.J1365;import java.util.Arrays;import java.util.HashMap;import java.util.Map;public class J1365 &#123;    public int[] smallerNumbersThanCurrent(int[] nums) &#123;        int[] nums2 = nums.clone();        Arrays.sort(nums2);        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();        for (int i = 0; i &lt; nums2.length; i++) &#123;            if (map.containsKey(nums2[i])) &#123;                int num = map.get(nums2[i]);                map.put(nums2[i], Math.min(num, i));            &#125; else &#123;                map.put(nums2[i], i);            &#125;        &#125;        int[] res = new int[nums.length];        for (int i = 0; i &lt; nums.length; i++) &#123;            res[i] = map.get(nums[i]);        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>哈希表</tag>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1354. Construct Target Array With Multiple Sums</title>
    <url>/2026/02/21/java/LeetCode-1354-Construct-Target-Array-With-Multiple-Sums/</url>
    <content><![CDATA[题目描述给你一个整数数组 target。一开始，你有一个数组 A，它的所有元素均为 1，你可以执行以下操作：

令 x 为你数组里所有元素的和。
选择满足 0 &lt;= i &lt; target.length 的任意下标 i，并让 A 数组中对应下标处的值为 x。

你可以重复该过程任意次。如果能从 A 开始构造出目标数组 target，返回 true，否则返回 false。
例如：target = [9,3,5] → true

初始 A &#x3D; [1,1,1]，sum &#x3D; 3，选择下标 0 → A &#x3D; [3,1,1]，sum &#x3D; 5
选择下标 2 → A &#x3D; [3,1,5]，sum &#x3D; 9
选择下标 0 → A &#x3D; [9,1,5]，sum &#x3D; 15
选择下标 1 → A &#x3D; [9,3,5]，等于 target

例如：target = [1,1,1,2] → false
解题思路该题的关键是逆向思维：与其从全 1 数组构造 target，不如从 target 反向推导回全 1 数组。
正向 vs 逆向正向操作：选择 i，将 A[i] 替换为数组总和 sum。这个操作会让选中的元素变大。
逆向操作：在 target 中，最大的元素一定是上一步被替换出来的。因为只有被选中的那个元素才会变成当前数组的总和，它一定大于数组中其他所有元素（其他元素没有被修改，而总和大于任意一个元素）。我们可以将最大的元素 max 还原为上一步的值。
逆向还原规则假设当前数组的总和为 sum，最大值为 max，其余元素的和为 rest = sum - max。
逆向一步，将 max 还原为它被替换前的值 prev = max - rest（因为替换前数组的总和 &#x3D; prev + rest，且替换后 max = prev + rest）。
算法步骤
初始化最大堆，将所有 target 元素插入堆中。同时计算总和 sum。
循环执行：
从堆中取出最大值 max。
计算剩余元素之和 rest = sum - max。
终止条件一：如果 max == 1 或 rest == 1，说明所有元素都可以回溯到 1，返回 true。
终止条件二（失败）：如果 rest &lt; 1（其余元素和为 0 或负数，不合理）、或者 max &lt;= rest（最大值不大于其余和，意味着逆向无法还原）、或者 max % rest == 0（还原后值为 0，不合理），返回 false。
取模优化：如果不使用取模，最大元素可能需要多次减 rest 才能小于等于其他元素。为了加速，直接用 max % rest 计算下一个值（如果 max % rest == 0 已经在前一步判断过了）。注意如果 max % rest == 0 且 rest == 1 是合法的（因为全 1 数组每一位更新时 rest = sum - 1），不过我们在前一步已通过 rest == 1 返回了 true。
计算 nextVal = max % rest，更新 sum = rest + nextVal，将 nextVal 插入堆中。


循环最终必然命中终止条件而返回结果。

为什么取模可以优化？考虑 target = [1, 1000000000]：如果没有取模，将 max 减去 rest 需要 1000000000 / 1 次操作，超时。而使用 max % rest，一步就能计算出最终还原值，极大提高了效率。
复杂度分析
时间复杂度：O(n log n + log M)，其中 n 是数组长度，M 是数组中最大值。建堆 O(n log n)，每次取模操作将一个很大的数至少减半，堆操作次数为 O(log M) 级别，每次 O(log n)。
空间复杂度：O(n)，最大堆存储所有元素。

代码package code.J1354;import datastructure.MyMaxHeap;public class J1354 &#123;    public boolean isPossible(int[] target) &#123;        MyMaxHeap&lt;Long&gt; maxHeap = new MyMaxHeap&lt;&gt;(target.length);        long sum = 0;        for (int i : target) &#123;            sum += i;            maxHeap.insert((long) i);        &#125;        while (true) &#123;            long max = maxHeap.extractMax();            long rest = sum - max;            if (max == 1 || rest == 1) return true;            if (rest &lt; 1 || max &lt;= rest || max % rest == 0) return false;            long nextVal = max % rest;            sum = rest + nextVal;            maxHeap.insert(nextVal);        &#125;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>堆</tag>
        <tag>Java</tag>
        <tag>最大堆</tag>
        <tag>数学</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 138. Copy List with Random Pointer</title>
    <url>/2026/02/28/java/LeetCode-138-Copy-List-with-Random-Pointer/</url>
    <content><![CDATA[题目描述给你一个长度为 n 的链表，每个节点包含一个额外增加的随机指针 random，该指针可以指向链表中的任何节点或空节点。构造这个链表的深拷贝。深拷贝应该正好由 n 个全新节点组成，其中每个新节点的值都设为其对应的原节点的值。新节点的 next 指针和 random 指针也都应指向新链表中的对应节点，从而使原链表和新链表中的这些指针能够表示相同的链表状态。
解题思路本题的难点在于处理 random 指针。由于 random 指针可以指向链表中的任意节点，在普通遍历中，我们可能还没有创建该目标节点，导致无法直接设置 random 指针。
使用哈希表可以优雅地解决这个问题，具体分为两趟遍历：
第一趟遍历：创建所有新节点，并建立原节点到新节点的映射关系（map&lt;原节点, 新节点&gt;）。此时新节点的 next 和 random 指针暂时为 null。这一步确保了所有节点都已经被创建，后续可以放心地引用它们。
第二趟遍历：再次遍历原链表，通过哈希表查找：

当前新节点的 next 应该指向 map.get(原节点.next)（即原节点 next 对应的新节点）
当前新节点的 random 应该指向 map.get(原节点.random)（即原节点 random 对应的新节点）

由于 HashMap 中 map.get(null) 返回 null，所以当 random 指向空时也能正确处理。
这种方法直观易懂，两趟遍历即可完成深拷贝。
复杂度分析
时间复杂度：O(n)，其中 n 是链表节点数。遍历了两次链表。
空间复杂度：O(n)，哈希表中存储了 n 个节点的映射关系。

代码package code.J138;import java.util.HashMap;import java.util.List;import java.util.Map;public class J138 &#123;    private static class Node &#123;        int val;        Node next;        Node random;        public Node(int val) &#123;            this.val = val;            this.next = null;            this.random = null;        &#125;    &#125;    public Node copyRandomList(Node head) &#123;        Map&lt;Node, Node&gt; map = new HashMap&lt;&gt;();        Node cur = head;        while (cur != null) &#123;            map.put(cur, new Node(cur.val));            cur = cur.next;        &#125;        cur = head;        while (cur != null) &#123;            Node newNode = map.get(cur);            newNode.next = map.get(cur.next);            newNode.random = map.get(cur.random);            cur = cur.next;        &#125;        return map.get(head);    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>哈希表</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1441. Build an Array With Stack Operations</title>
    <url>/2026/02/14/java/LeetCode-1441-Build-an-Array-With-Stack-Operations/</url>
    <content><![CDATA[题目描述给你一个目标数组 target 和一个整数 n。每次迭代，需要从 list = {1,2,3...,n} 中读取一个数字。使用栈操作 Push 和 Pop，构建目标数组 target。返回构建 target 所用的操作序列。
例如：target = [1,3]，n = 3，输出 [&quot;Push&quot;,&quot;Push&quot;,&quot;Pop&quot;,&quot;Push&quot;]。
解释：读取 1，Push（得到 [1]）；读取 2，Push（得到 [1,2]），但 2 不在 target 中，所以 Pop（得到 [1]）；读取 3，Push（得到 [1,3]），3 在 target 中。
解题思路核心思想题目本质是一个模拟问题。我们按顺序从 1 到 n 遍历数字，对于每个数字 i：

无论 i 是否在 target 中，我们都需要先执行 Push。
如果 i 不在 target 中，则紧接着执行 Pop（相当于 Push 之后马上 Pop 掉）。
如果 i 在 target 中，则保留，继续处理下一个数字。
当 target 中的所有数字都已处理完毕时，即可停止。

步骤分解
构建标记数组：创建一个长度为 n 的布尔标记数组 ans，将 target 中出现的元素对应位置标记为 1。
找到最大下标：遍历标记数组，找到 target 中最后一个元素在标记数组中的位置 index。我们只需要处理到该位置即可，因为之后的数字不需要 Push。
模拟操作：从 0 到 index 遍历标记数组：
每个元素都 Push。
如果该元素不在 target 中（标记为 0），则 Pop。



为什么只需要处理到 target 中最大元素的位置？因为一旦处理完 target 中最大的数字，后续更大的数字不需要 Push（它们不会成为 target 的一部分），所以可以直接停止。
以 target = [1,3]，n = 3 为例：

target 中最大元素是 3，位置在 index&#x3D;2
遍历 i&#x3D;0（数字 1标记&#x3D;1）→ Push
i&#x3D;1（数字 2标记&#x3D;0）→ Push, Pop
i&#x3D;2（数字 3标记&#x3D;1）→ Push
结果：[&quot;Push&quot;,&quot;Push&quot;,&quot;Pop&quot;,&quot;Push&quot;]

复杂度分析
时间复杂度：O(n)，遍历标记数组一次。
空间复杂度：O(n)，需要标记数组和结果列表的空间。

代码package code.J1441;import java.util.ArrayList;import java.util.List;public class J1441 &#123;    public List&lt;String&gt; buildArray(int[] target, int n) &#123;        List&lt;String&gt; res = new ArrayList&lt;&gt;();        int[] ans = new int[n];        for (int i : target) &#123;            ans[i - 1] = 1;        &#125;        int index = 0;        for (int i = 0; i &lt; ans.length; i++) &#123;            if (ans[i] == 1) index = i;        &#125;        for (int i = 0; i &lt;= index; i++) &#123;            res.add(&quot;Push&quot;);            if (ans[i] == 0) res.add(&quot;Pop&quot;);        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>栈</tag>
        <tag>模拟</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1470. Shuffle the Array</title>
    <url>/2026/02/14/java/LeetCode-1470-Shuffle-the-Array/</url>
    <content><![CDATA[题目描述给你一个数组 nums，数组中有 2n 个元素，按 [x1,x2,...,xn,y1,y2,...,yn] 的格式排列。请你将数组按 [x1,y1,x2,y2,...,xn,yn] 格式重新排列，返回重排后的数组。
例如：nums = [2,5,1,3,4,7]，n = 3，输出 [2,3,5,4,1,7]。
解释：x1=2, x2=5, x3=1，y1=3, y2=4, y3=7，交错排列得 [2,3,5,4,1,7]。
解题思路核心思想这是一个数组重排问题，本质是双指针或交错填充。给定 n，我们知道：

x 部分的元素在 nums[0] 到 nums[n-1]。
y 部分的元素在 nums[n] 到 nums[2n-1]。

目标是将两个部分交错：x1, y1, x2, y2, ..., xn, yn。
步骤
创建结果数组 ans，长度为 2n。
使用指针 i 在结果数组中填充，使用指针 j 遍历 x 部分和 y 部分：
每次迭代，先放入 nums[j]（来自 x 部分），然后放入 nums[j + n]（来自 y 部分）。
j 从 0 到 n-1。



示例以 nums = [2,5,1,3,4,7]，n = 3 为例：

j&#x3D;0：ans[0]&#x3D;nums[0]&#x3D;2, ans[1]&#x3D;nums[3]&#x3D;3
j&#x3D;1：ans[2]&#x3D;nums[1]&#x3D;5, ans[3]&#x3D;nums[4]&#x3D;4
j&#x3D;2：ans[4]&#x3D;nums[2]&#x3D;1, ans[5]&#x3D;nums[5]&#x3D;7
结果：[2,3,5,4,1,7]

复杂度分析
时间复杂度：O(n)，一次遍历即可完成。
空间复杂度：O(n)（不计结果数组），结果数组长度为 2n。

代码package code.J1470;public class J1470 &#123;    public int[] shuffle(int[] nums, int n) &#123;        int[] ans = new int[2 * n];        int i = 0;        for (int j = 0; j &lt; n; j++) &#123;            ans[i++] = nums[j];            ans[i++] = nums[j + n];        &#125;        return ans;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1475. Final Prices With a Special Discount in a Shop</title>
    <url>/2026/02/14/java/LeetCode-1475-Final-Prices-With-a-Special-Discount-in-a-Shop/</url>
    <content><![CDATA[题目描述给你一个数组 prices，其中 prices[i] 是商店里第 i 件商品的价格。商店里正在进行促销活动，如果你要买第 i 件商品，那么你可以得到与 prices[j] 相等的折扣，其中 j 是满足 j &gt; i 且 prices[j] &lt;= prices[i] 的最小下标，如果没有满足条件的 j，你将没有任何折扣。请你返回一个数组，数组中第 i 个元素是折扣后你购买商品 i 最终需要支付的价格。
例如：prices = [8,4,6,2,3]，输出 [4,2,4,2,3]。
解释：商品 0 价格 8，右侧第一个 &lt;&#x3D; 8 的是价格 4，最终支付 8-4&#x3D;4；商品 1 价格 4，右侧第一个 &lt;&#x3D; 4 的是价格 2，最终支付 4-2&#x3D;2；商品 2 价格 6，右侧第一个 &lt;&#x3D; 6 的是价格 2，最终支付 6-2&#x3D;4；商品 3 价格 2，右侧无 &lt;&#x3D; 2 的元素，最终支付 2；商品 4 价格 3，右侧无元素，最终支付 3。
解题思路暴力法对每个元素，向右侧扫描寻找第一个不大于它的元素，时间复杂度 O(n²)。
单调栈解法（O(n)）本题是寻找右侧第一个小于等于当前元素的元素的经典问题，可以用单调递增栈解决。
核心思想：

初始化结果数组 res，默认值为 prices[i]（即无折扣时的价格）。
维护一个从栈底到栈顶递增的栈，栈中存储下标。
遍历 prices，对于当前商品 i：
当栈不为空且当前价格 prices[i] &lt;&#x3D; 栈顶下标对应的价格时：
说明栈顶商品找到了最近的 &lt;&#x3D; 它的价格，可以享受折扣。
弹出栈顶下标 index，计算 res[index] = prices[index] - prices[i]。


将当前下标 i 入栈。


遍历结束后，栈中剩余元素没有折扣，res 中保持原价即可。

为什么单调栈可以解决问题？以 [8,4,6,2,3] 为例，模拟过程：

i&#x3D;0（价格 8）：栈空，入栈 [0]
i&#x3D;1（价格 4）：4 &lt; 8，弹出 0，res[0]&#x3D;8-4&#x3D;4；入栈 [1]
i&#x3D;2（价格 6）：6 &gt; 4，不弹出；入栈 [1,2]
i&#x3D;3（价格 2）：2 &lt; 6，弹出 2，res[2]&#x3D;6-2&#x3D;4；2 &lt; 4，弹出 1，res[1]&#x3D;4-2&#x3D;2；入栈 [3]
i&#x3D;4（价格 3）：3 &gt; 2，不弹出；入栈 [3,4]
结束，栈中 [3,4] 无折扣，最终为 [4,2,4,2,3]

每个元素最多进栈一次、出栈一次，总时间复杂度 O(n)。
复杂度分析
时间复杂度：O(n)，每个元素最多入栈和出栈各一次。
空间复杂度：O(n)，最坏情况下栈中存储所有元素下标。

代码package code.J1475;import java.util.ArrayDeque;import java.util.Deque;public class J1475 &#123;    public int[] finalPrices(int[] prices) &#123;        int[] res = new int[prices.length];        Deque&lt;Integer&gt; stack = new ArrayDeque&lt;&gt;();        for (int i = 0; i &lt; prices.length; i++) &#123;            res[i] = prices[i];            while (!stack.isEmpty() &amp;&amp; prices[stack.peek()] &gt;= prices[i]) &#123;                int index = stack.pop();                res[index] = prices[index] - prices[i];            &#125;            stack.push(i);        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>单调栈</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 150. Evaluate Reverse Polish Notation</title>
    <url>/2026/02/14/java/LeetCode-150-Evaluate-Reverse-Polish-Notation/</url>
    <content><![CDATA[题目描述根据逆波兰表示法（Reverse Polish Notation，RPN），求表达式的值。有效的算符包括 +、-、*、/。每个运算对象可以是整数，也可以是另一个逆波兰表达式。注意两个整数之间的除法只保留整数部分。可以保证给定的逆波兰表达式总是有效的。
解题思路逆波兰表达式是一种后缀表达式，其特点是将运算符放在操作数之后。计算逆波兰表达式非常适合使用栈数据结构。
核心思路：遍历 tokens 数组中的每一个字符串：

如果当前字符串是数字（操作数），直接将其转换为整数并入栈。
如果当前字符串是运算符（+、-、*、/），则从栈中弹出两个操作数（注意弹出顺序：先弹出的是第二个操作数 num2，后弹出的是第一个操作数 num1），根据运算符执行相应的计算，并将计算结果压回栈中。

当遍历完所有 token 后，栈中剩下的唯一一个元素就是整个表达式的计算结果。
关键细节：

由于减法和除法不满足交换律，操作数的顺序很重要。在逆波兰表达式中，遇到运算符时，栈顶元素是右操作数，次栈顶元素是左操作数。例如对于 [&quot;4&quot;, &quot;3&quot;, &quot;-&quot;]，先入栈 4，再入栈 3，遇到 - 时弹出 3 作为右操作数、4 作为左操作数，计算 4 - 3 &#x3D; 1。
除法按题目要求保留整数部分，Java 中的整数除法天然满足这个要求。

复杂度分析
时间复杂度：O(n)，其中 n 是 tokens 数组的长度。只需要遍历一次数组，每次入栈、出栈操作为 O(1)。
空间复杂度：O(n)，栈中最多存储 n 个操作数。

代码package code.J150;import java.util.ArrayDeque;import java.util.Deque;public class J150 &#123;    public int evalRPN(String[] tokens) &#123;        Deque&lt;Integer&gt; stack = new ArrayDeque&lt;&gt;();        for (String s : tokens) &#123;            if (&quot;+&quot;.equals(s) || &quot;-&quot;.equals(s) || &quot;*&quot;.equals(s) || &quot;/&quot;.equals(s)) &#123;                int num2 = stack.pop();                int num1 = stack.pop();                switch (s) &#123;                    case &quot;+&quot;: stack.push(num1 + num2); break;                    case &quot;-&quot;: stack.push(num1 - num2); break;                    case &quot;*&quot;: stack.push(num1 * num2); break;                    case &quot;/&quot;: stack.push(num1 / num2); break;                &#125;            &#125; else &#123;                stack.push(Integer.parseInt(s));            &#125;        &#125;        return stack.pop();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>栈</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1507. Reformat Date</title>
    <url>/2026/02/25/java/LeetCode-1507-Reformat-Date/</url>
    <content><![CDATA[题目描述给你一个字符串 date，它的格式为 Day Month Year，其中 Day 是集合 {&quot;1st&quot;, &quot;2nd&quot;, &quot;3rd&quot;, &quot;4th&quot;, ..., &quot;30th&quot;, &quot;31st&quot;} 中的一个元素，Month 是集合 {&quot;Jan&quot;, &quot;Feb&quot;, &quot;Mar&quot;, &quot;Apr&quot;, &quot;May&quot;, &quot;Jun&quot;, &quot;Jul&quot;, &quot;Aug&quot;, &quot;Sep&quot;, &quot;Oct&quot;, &quot;Nov&quot;, &quot;Dec&quot;} 中的一个元素，Year 的范围在 [1900, 2100] 之间。请你将字符串转变为 YYYY-MM-DD 的格式。
例如：date = &quot;20th Oct 2052&quot;，输出 &quot;2052-10-20&quot;。
解题思路核心思想这是一个简单的字符串解析与格式化问题。输入格式为 &quot;Day Month Year&quot;，需要输出 &quot;YYYY-MM-DD&quot;。
步骤
字符串分割：使用 split(&quot; &quot;) 将输入日期按空格分割为三个部分——日、月、年。
年份直接拼接：年份部分（dateArr[2]）已经是数字字符串，直接使用。
月份转换：使用一个 HashMap 存储月份的英文缩写到数字的映射（如 &quot;Jan&quot; → &quot;01&quot;），通过查表快速转换。
日期处理：日期部分包含英文序数后缀（&quot;st&quot;, &quot;nd&quot;, &quot;rd&quot;, &quot;th&quot;），需要提取纯数字：
如果日期长度为 3（即个位数如 &quot;1st&quot;），取第一个字符并在前面补 &quot;0&quot;。
如果日期长度为 4（即两位数如 &quot;20th&quot;），取前两个字符。


拼接输出：按 YYYY-MM-DD 格式用 StringBuilder 拼接。

示例说明以 &quot;20th Oct 2052&quot; 为例：

分割得到 [&quot;20th&quot;, &quot;Oct&quot;, &quot;2052&quot;]
Year &#x3D; &quot;2052&quot;
Month &#x3D; &quot;Oct&quot; → 查表得 &quot;10&quot;
Day &#x3D; &quot;20th&quot; → 取前两个字符 &quot;20&quot;
拼接结果：&quot;2052-10-20&quot;

复杂度分析
时间复杂度：O(1)，输入长度固定，所有操作均为常数时间。
空间复杂度：O(1)，除结果字符串外仅使用常数额外空间（HashMap 固定大小）。

代码package code.J1507;import java.util.HashMap;import java.util.Map;public class J1507 &#123;    static Map&lt;String, String&gt; monthMap = new HashMap&lt;&gt;();    static &#123;        monthMap.put(&quot;Jan&quot;, &quot;01&quot;); monthMap.put(&quot;Feb&quot;, &quot;02&quot;); monthMap.put(&quot;Mar&quot;, &quot;03&quot;);        monthMap.put(&quot;Apr&quot;, &quot;04&quot;); monthMap.put(&quot;May&quot;, &quot;05&quot;); monthMap.put(&quot;Jun&quot;, &quot;06&quot;);        monthMap.put(&quot;Jul&quot;, &quot;07&quot;); monthMap.put(&quot;Aug&quot;, &quot;08&quot;); monthMap.put(&quot;Sep&quot;, &quot;09&quot;);        monthMap.put(&quot;Oct&quot;, &quot;10&quot;); monthMap.put(&quot;Nov&quot;, &quot;11&quot;); monthMap.put(&quot;Dec&quot;, &quot;12&quot;);    &#125;    public String reformatDate(String date) &#123;        String[] dateArr = date.split(&quot; &quot;);        StringBuilder sb = new StringBuilder();        sb.append(dateArr[2]);        sb.append(&quot;-&quot;);        sb.append(monthMap.get(dateArr[1]));        sb.append(&quot;-&quot;);        if (dateArr[0].length() == 3) sb.append(&quot;0&quot;).append(dateArr[0].charAt(0));        else sb.append(dateArr[0], 0, 2);        return sb.toString();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1590. Make Sum Divisible by P</title>
    <url>/2026/02/28/java/LeetCode-1590-Make-Sum-Divisible-by-P/</url>
    <content><![CDATA[题目描述给你一个正整数数组 nums，请你移除最短子数组（可以为空），使得剩余元素的和能被 p 整除。不允许将整个数组都移除。返回你需要移除的最短子数组的长度，如果无法满足题目要求，返回 -1。
例如：nums = [3,1,4,2]，p = 6，输出 1。
解释：数组总和为 3+1+4+2=10，10 % 6 = 4。我们需要移除一个和为 4 的子数组。移除子数组 [4]（索引 2），剩余元素和 3+1+2=6，能被 6 整除。子数组长度为 1，是最短的。
解题思路核心数学原理设数组总和模 p 的值为 totalSum % p = target：

如果 target == 0，说明整个数组的和已经能被 p 整除，直接返回 0。
否则，我们需要移除一个子数组，使得该子数组的和模 p 等于 target，这样剩余元素的和模 p 就等于 0。

为什么？ 因为剩余和 &#x3D; 总和 - 子数组和。若剩余和能被 p 整除，即 (totalSum - subSum) % p == 0，则 subSum % p == totalSum % p == target。
前缀和 + 哈希表问题转化为：寻找最短的连续子数组，其和模 p 等于 target。
设 prefix[i] 为 nums[0..i] 的前缀和模 p。如果存在 j &lt; i 使得 (prefix[i] - prefix[j]) % p == target，则子数组 nums[j+1..i] 满足条件。
即我们需要 prefix[j] == (prefix[i] - target) % p（注意处理负数取模）。
算法步骤：

计算 totalSum % p 得到 target。如果为 0，返回 0。
使用哈希表 map 记录每个前缀和模值最后一次出现的位置（或第一次出现的位置），初始化 map.put(0, -1)。
遍历数组，维护当前前缀和模值 curSum：
计算需要的值 needed = (curSum - target + p) % p。
如果 map 中存在 needed，说明找到了符合条件的子数组 [map.get(needed)+1, i]，更新最小长度。
将 (curSum, i) 存入 map（记录当前位置，确保能找到最短子数组）。


返回最小长度，若为 n 则返回 -1。

示例说明以 nums = [3,1,4,2]，p = 6 为例：

totalSum &#x3D; 10，target &#x3D; 4
map &#x3D; {0: -1}，curSum &#x3D; 0
i&#x3D;0（3）：curSum &#x3D; 3，needed &#x3D; (3-4+6)%6 &#x3D; 5，不在 map；map &#x3D; {0:-1, 3:0}
i&#x3D;1（1）：curSum &#x3D; 4，needed &#x3D; (4-4+6)%6 &#x3D; 0，在 map（位置 -1）→ 长度 &#x3D; 1-(-1) &#x3D; 2；map &#x3D; {0:-1, 3:0, 4:1}
i&#x3D;2（4）：curSum &#x3D; 2，needed &#x3D; (2-4+6)%6 &#x3D; 4，在 map（位置 1）→ 长度 &#x3D; 2-1 &#x3D; 1，更新 minLen &#x3D; 1；map &#x3D; {…, 2:2}
i&#x3D;3（2）：curSum &#x3D; 4，needed &#x3D; 0，在 map（位置 -1）→ 长度 &#x3D; 3-(-1) &#x3D; 4；minLen &#x3D; 1
返回 1

复杂度分析
时间复杂度：O(n)，一次遍历完成。
空间复杂度：O(n)，哈希表最多存储 n 个前缀和模值。

代码package code.J1590;import java.util.HashMap;import java.util.Map;public class J1590 &#123;    public int minSubarray(int[] nums, int p) &#123;        int n = nums.length;        int totalSum = 0;        for (int num : nums) &#123;            totalSum = (totalSum + num) % p;        &#125;        int target = totalSum;        if (target == 0) return 0;        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();        map.put(0, -1);        int curSum = 0;        int minLen = n;        for (int i = 0; i &lt; n; i++) &#123;            curSum = (curSum + nums[i]) % p;            int needed = (curSum - target + p) % p;            if (map.containsKey(needed)) &#123;                minLen = Math.min(minLen, i - map.get(needed));            &#125;            map.put(curSum, i);        &#125;        return minLen == n ? -1 : minLen;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>哈希表</tag>
        <tag>数学</tag>
        <tag>前缀和</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1668. Maximum Repeating Substring</title>
    <url>/2026/02/25/java/LeetCode-1668-Maximum-Repeating-Substring/</url>
    <content><![CDATA[题目描述给你一个字符串 sequence，如果字符串 word 连续重复 k 次形成的字符串是 sequence 的一个子字符串，那么单词 word 的重复值为 k。单词 word 的最大重复值是单词 word 在 sequence 中最大的重复值。返回单词 word 的最大重复值。
例如：sequence = &quot;ababc&quot;，word = &quot;ab&quot;，输出 2。
解释：&quot;abab&quot;（word 重复 2 次）是 &quot;ababc&quot; 的子串；但 &quot;ababab&quot;（重复 3 次）不是 &quot;ababc&quot; 的子串。所以最大重复值为 2。
解题思路核心思想要找到 word 重复 k 次后成为 sequence 子串的最大 k 值，我们可以从小到大尝试：

不断将 word 拼接自身，检查拼接后的字符串是否是 sequence 的子串。
只要匹配，就增加计数，继续尝试。
直到匹配失败则停止。

步骤
初始化计数器 res = 0，保存原始字符串 wordBK = word。
进入循环：如果 sequence 包含 word：
计数 res++。
将 word 拼接一次自身（word += wordBK），尝试下一个更大的 k 值。


返回 res。

为什么这种方法可行？每次拼接后，word 的长度增加 wordBK.length()。当 word 的长度超过 sequence 的长度时，contains() 必然返回 false，循环自动终止。
重复 k 次形成的字符串长度是 k * |word|，因此 k 的最大可能值为 |sequence| / |word|，最多尝试 O(n&#x2F;m) 次。
示例说明以 sequence = &quot;ababc&quot;，word = &quot;ab&quot; 为例：

word &#x3D; “ab”：sequence 包含 “ab” ✓，res&#x3D;1，word &#x3D; “abab”
word &#x3D; “abab”：sequence 包含 “abab” ✓，res&#x3D;2，word &#x3D; “ababab”
word &#x3D; “ababab”：长度 6 &gt; sequence 长度 5，不包含 ✗
返回 2

复杂度分析
时间复杂度：O(n² / m)，最坏情况下需要检查 n/m 次 contains()，每次 O(n)。
空间复杂度：O(n)，拼接的字符串长度最多达到 n。

代码package code.J1668;public class J1668 &#123;    public int maxRepeating(String sequence, String word) &#123;        int res = 0;        String wordBK = word;        while (sequence.contains(word)) &#123;            res++;            word += wordBK;        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1664. Ways to Make a Fair Array</title>
    <url>/2026/02/28/java/LeetCode-1664-Ways-to-Make-a-Fair-Array/</url>
    <content><![CDATA[题目描述给你一个整数数组 nums。你需要恰好移除一个元素后，使得剩余元素中，奇数下标元素之和等于偶数下标元素之和。返回满足条件的移除方案数。
例如：nums = [2,1,6,4]，输出 1。
解释：

移除下标 0：[1,6,4]，偶数下标和 &#x3D; 1+4&#x3D;5，奇数下标和 &#x3D; 6，5 ≠ 6
移除下标 1：[2,6,4]，偶数下标和 &#x3D; 2+4&#x3D;6，奇数下标和 &#x3D; 6，6 &#x3D; 6 ✓
移除下标 2：[2,1,4]，偶数下标和 &#x3D; 2+4&#x3D;6，奇数下标和 &#x3D; 1，6 ≠ 1
移除下标 3：[2,1,6]，偶数下标和 &#x3D; 2+6&#x3D;8，奇数下标和 &#x3D; 1，8 ≠ 1只有移除下标 1 满足条件。

解题思路核心思想直接枚举每个元素被移除的情况需要 O(n²)。我们可以用前缀和的思想在 O(n) 时间内解决。
当移除下标 i 的元素后，剩余元素的下标会发生变化：

i 之前的元素，其下标奇偶性不变。
i 之后的元素，其下标奇偶性反转（原来的偶数变奇数，奇数变偶数）。

因此，移除 i 之后：

新的偶数下标和 &#x3D; i 之前的偶数下标和 + i 之后的奇数下标和
新的奇数下标和 &#x3D; i 之前的奇数下标和 + i 之后的偶数下标和（注意减去被移除的 nums[i]，如果 i 是偶数，则从偶数和中减去；如果 i 是奇数，则从奇数和中减去）

步骤
先统计整个数组的偶数下标总和 evenTotal 和奇数下标总和 oddTotal。
遍历数组，维护 evenPrefix（当前元素之前的偶数下标前缀和）和 oddPrefix（当前元素之前的奇数下标前缀和）。
对于每个下标 i，分两种情况进行计算：
如果 i 是偶数下标：
移除后偶数下标和 &#x3D; evenPrefix + （i 之后的奇数下标总和）&#x3D; evenPrefix + (oddTotal - oddPrefix)
移除后奇数下标和 &#x3D; oddPrefix + （i 之后的偶数下标总和）- nums[i] &#x3D; oddPrefix + (evenTotal - evenPrefix - nums[i])


如果 i 是奇数下标：
移除后偶数下标和 &#x3D; evenPrefix + （i 之后的奇数下标总和）- nums[i] &#x3D; evenPrefix + (oddTotal - oddPrefix - nums[i])
移除后奇数下标和 &#x3D; oddPrefix + （i 之后的偶数下标总和）&#x3D; oddPrefix + (evenTotal - evenPrefix)




如果移除后的偶数下标和 &#x3D;&#x3D; 奇数下标和，ans++。
更新 evenPrefix 或 oddPrefix（根据 i 的奇偶性）。

示例说明以 nums = [2,1,6,4] 为例：

evenTotal &#x3D; nums[0] + nums[2] &#x3D; 2 + 6 &#x3D; 8
oddTotal &#x3D; nums[1] + nums[3] &#x3D; 1 + 4 &#x3D; 5
i&#x3D;0（偶数）：新偶 &#x3D; 0+(5-0) &#x3D; 5，新奇 &#x3D; 0+(8-0-2) &#x3D; 6，5≠6；更新 evenPrefix &#x3D; 2
i&#x3D;1（奇数）：新偶 &#x3D; 2+(5-0-1) &#x3D; 6，新奇 &#x3D; 0+(8-2) &#x3D; 6，6&#x3D;6 ✓，ans&#x3D;1；更新 oddPrefix &#x3D; 1
i&#x3D;2（偶数）：新偶 &#x3D; 2+(5-1) &#x3D; 6，新奇 &#x3D; 1+(8-2-6) &#x3D; 1，6≠1；更新 evenPrefix &#x3D; 8
i&#x3D;3（奇数）：新偶 &#x3D; 8+(5-1-4) &#x3D; 8，新奇 &#x3D; 1+(8-8) &#x3D; 1，8≠1；更新 oddPrefix &#x3D; 5
返回 1

复杂度分析
时间复杂度：O(n)，两次遍历数组。
空间复杂度：O(1)，只使用常数额外空间。

代码package code.J1664;public class J1664 &#123;    public int waysToMakeFair(int[] nums) &#123;        int n = nums.length;        int oddTotal = 0, evenTotal = 0;        for (int i = 0; i &lt; n; i++) &#123;            if (i % 2 == 0) evenTotal += nums[i];            else oddTotal += nums[i];        &#125;        int ans = 0;        int oddPrefix = 0, evenPrefix = 0;        for (int i = 0; i &lt; n; i++) &#123;            int currentEvenSum, currentOddSum;            if (i % 2 == 0) &#123;                currentEvenSum = evenPrefix + (oddTotal - oddPrefix);                currentOddSum = oddPrefix + (evenTotal - evenPrefix - nums[i]);            &#125; else &#123;                currentEvenSum = evenPrefix + (oddTotal - oddPrefix - nums[i]);                currentOddSum = oddPrefix + (evenTotal - evenPrefix);            &#125;            if (currentEvenSum == currentOddSum) ans++;            if (i % 2 == 0) evenPrefix += nums[i];            else oddPrefix += nums[i];        &#125;        return ans;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>前缀和</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1705. Maximum Number of Eaten Apples</title>
    <url>/2026/02/25/java/LeetCode-1705-Maximum-Number-of-Eaten-Apples/</url>
    <content><![CDATA[题目描述有一棵特殊的苹果树，一连 n 天，每天都可以长出若干个苹果。在第 i 天，树上会长出 apples[i] 个苹果，这些苹果将会在 days[i] 天后腐烂，变得无法食用。你打算每天最多吃一个苹果，以保持营养均衡。返回你可以吃掉的苹果的最大数目。
例如：apples = [1,2,3,5,2]，days = [3,2,1,4,2]，输出 7。
解题思路核心贪心策略这是一个经典的贪心 + 最小堆问题。为了最大化吃掉的苹果数量，每天应该优先吃掉最早腐烂的苹果（即过期时间最早的苹果）。这样可以避免苹果在腐烂前没有被吃掉。
数据结构定义一个 Apple 类，包含两个属性：

count：该批次苹果的剩余数量。
days：该批次苹果的过期时间（即 i + days[i]），在 days 天之前（不含当天）可以食用。

使用最小堆按过期时间 days 排序，堆顶始终是即将最早腐烂的苹果。
算法步骤
初始化最小堆 heap，计数器 res = 0，天数 i = 0。
当 i &lt; n（还有苹果在生长）或堆非空（还有苹果可以吃）时进行循环：
添加当天的苹果：如果 i &lt; n 且 apples[i] &gt; 0，将 (apples[i], i + days[i]) 插入堆中。
清理过期苹果：不断弹出堆顶，如果堆顶苹果的过期时间 &lt;= i（即已经过期），将其丢弃；否则放回堆中并停止清理。
吃苹果：如果堆非空，弹出堆顶（最早过期的苹果）：
res++（吃掉一个）。
该批次剩余数量减 1，如果还有剩余，重新插入堆中。


i++，进入下一天。


返回 res。

示例说明以 apples = [1,2,3,5,2]，days = [3,2,1,4,2] 为例（部分模拟）：

第 0 天：添加苹果 (1个，第3天过期)。清理：无过期。吃：吃1个。res&#x3D;1。
第 1 天：添加苹果 (2个，第3天过期)。清理：无过期。吃：吃最早过期的（第3天那批）。res&#x3D;2。
…依此类推。

为什么贪心是正确的？如果我们不优先吃最早腐烂的苹果，那么一些即将腐烂的苹果就会浪费掉。贪心选择总是消耗”最紧迫”的苹果，保证不浪费任何可以在腐烂前被吃掉的苹果。
复杂度分析
时间复杂度：O((n + res) * log n)，每天最多进行一次插入和删除操作，堆的大小不超过 n。
空间复杂度：O(n)，堆中最多存储 n 个批次的苹果。

代码package code.J1705;import datastructure.MyMinHeap;class Apple implements Comparable&lt;Apple&gt; &#123;    private int count;    private int days;    public Apple(int count, int days) &#123; this.count = count; this.days = days; &#125;    public int getCount() &#123; return count; &#125;    public void setCount(int count) &#123; this.count = count; &#125;    public int getDays() &#123; return days; &#125;    public void setDays(int days) &#123; this.days = days; &#125;    @Override    public int compareTo(Apple o) &#123; return this.days - o.getDays(); &#125;&#125;public class J1705 &#123;    public int eatenApples(int[] apples, int[] days) &#123;        MyMinHeap&lt;Apple&gt; heap = new MyMinHeap&lt;&gt;(apples.length);        int res = 0;        int i = 0;        int n = apples.length;        while (i &lt; n || !heap.isEmpty()) &#123;            if (i &lt; n &amp;&amp; apples[i] &gt; 0) &#123;                heap.insert(new Apple(apples[i], i + days[i]));            &#125;            while (!heap.isEmpty()) &#123;                Apple top = heap.extractMin();                if (top.getDays() &gt; i) &#123; heap.insert(top); break; &#125;            &#125;            if (!heap.isEmpty()) &#123;                Apple earliest = heap.extractMin();                res++;                earliest.setCount(earliest.getCount() - 1);                if (earliest.getCount() &gt; 0) heap.insert(earliest);            &#125;            i++;        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>堆</tag>
        <tag>最小堆</tag>
        <tag>Java</tag>
        <tag>贪心</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1732. Find the Highest Altitude</title>
    <url>/2026/02/28/java/LeetCode-1732-Find-the-Highest-Altitude/</url>
    <content><![CDATA[题目描述有一个自行车手打算进行一场公路骑行，这条路线总共由 n + 1 个不同海拔的点组成。自行车手从海拔为 0 的点 0 开始骑行。给你一个长度为 n 的整数数组 gain，其中 gain[i] 是点 i 和点 i + 1 的净海拔高度差（0 &lt;= i &lt; n）。请你返回最高点的海拔。
例如：gain = [-5,1,5,0,-7]，输出 1。
解释：海拔变化：0 → -5 → -4 → 1 → 1 → -6，最高海拔为 1。
解题思路核心思想这是一个典型的前缀和问题。给定相邻两点之间的高度差数组 gain，我们需要求出所有点的海拔值中的最大值。
设点 0 的海拔 altitude[0] = 0，则：

altitude[1] = altitude[0] + gain[0]
altitude[2] = altitude[1] + gain[1]
…
altitude[i+1] = altitude[i] + gain[i]

每个点的海拔都是前一个点的海拔加上对应的高度差，这正是前缀和。
步骤
创建一个长度为 n+1 的数组 prefixAndGain，其中 prefixAndGain[0] = 0。
遍历 gain，计算每个点的海拔：prefixAndGain[i+1] = prefixAndGain[i] + gain[i]。
遍历 prefixAndGain，记录最大值并返回。

空间优化实际上不需要存储所有前缀和：

维护一个变量 currentAltitude，初始为 0。
维护 maxAltitude，初始为 0（因为起点海拔为 0）。
每步更新 currentAltitude += gain[i]，然后更新 maxAltitude = max(maxAltitude, currentAltitude)。

示例说明以 gain = [-5,1,5,0,-7] 为例：

起点海拔 &#x3D; 0，max &#x3D; 0
gain[0] &#x3D; -5 → 海拔 &#x3D; -5，max &#x3D; 0
gain[1] &#x3D; 1 → 海拔 &#x3D; -4，max &#x3D; 0
gain[2] &#x3D; 5 → 海拔 &#x3D; 1，max &#x3D; 1
gain[3] &#x3D; 0 → 海拔 &#x3D; 1，max &#x3D; 1
gain[4] &#x3D; -7 → 海拔 &#x3D; -6，max &#x3D; 1
返回 1

复杂度分析
时间复杂度：O(n)，一次遍历即可。
空间复杂度：O(n)（代码中使用前缀数组）或 O(1)（优化后仅使用变量）。

代码package code.J1732;public class J1732 &#123;    public int largestAltitude(int[] gain) &#123;        int[] prefixAndGain = new int[gain.length + 1];        for (int i = 0; i &lt; gain.length; i++) &#123;            prefixAndGain[i + 1] = prefixAndGain[i] + gain[i];        &#125;        int max = Integer.MIN_VALUE;        for (int j : prefixAndGain) max = Math.max(max, j);        return max;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>前缀和</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1700. Number of Students Unable to Eat Lunch</title>
    <url>/2026/02/21/java/LeetCode-1700-Number-of-Students-Unable-to-Eat-Lunch/</url>
    <content><![CDATA[题目描述学校的自助午餐提供圆形和方形的三明治，分别用 0 和 1 表示。所有学生站在一个队列里，每个学生要么喜欢圆形的要么喜欢方形的。餐厅里三明治的数量与学生的数量相同。所有三明治都放在一个栈里，每一轮：

如果队列最前面的学生喜欢栈顶的三明治，会拿走它并离开队列；
否则学生会放弃这个三明治并回到队列的尾部。这个过程会一直持续到队列里所有学生都不喜欢栈顶的三明治为止。返回无法吃上午餐的学生数量。

例如：students = [1,1,0,0]，sandwiches = [0,1,0,1]，输出 0。
解释：所有学生最终都能拿到喜欢的三明治。
解题思路核心思想这是一个模拟问题，使用队列来模拟学生的排队行为。
步骤
将所有学生放入队列 queue 中。
遍历三明治栈（sandwiches），对于每个三明治：
记录已检查的学生数 r = 0。
不断检查队首学生是否喜欢当前三明治：
如果喜欢（queue.peek() == sandwich），该学生拿走三明治并出队，跳出内层循环。
如果不喜欢，该学生回到队尾（queue.offer(queue.poll())），r++。
关键：如果 r == queue.size()，说明所有剩余学生都不喜欢当前栈顶三明治，此后的学生都无法吃饭，直接返回 queue.size()。


如果学生喜欢三明治，出队（该学生已吃饭），继续处理下一个三明治。


返回 queue.size()。

为什么需要内层循环计数 r？学生在队列中循环，如果一轮遍历完所有学生后仍然没有人喜欢栈顶的三明治，那么之后的循环也无法改变结果（学生不变，三明治不变），因此可以提前终止。
示例说明以 students = [1,1,0,0]，sandwiches = [0,1,0,1] 为例：

队列：[1,1,0,0]，栈顶三明治&#x3D;0
队首&#x3D;1，不喜欢，转到队尾：[1,0,0,1]
队首&#x3D;1，不喜欢，转到队尾：[0,0,1,1]
队首&#x3D;0，喜欢！出队，三明治消耗
队列：[0,1,1]，栈顶三明治&#x3D;1
队首&#x3D;0，不喜欢，转到队尾：[1,1,0]
队首&#x3D;1，喜欢！出队
依此类推…最终 queue 为空，返回 0。

复杂度分析
时间复杂度：O(n²)，每个学生可能被反复移到队尾多次。
空间复杂度：O(n)，队列存储所有学生。

代码package code.J1700;import java.util.LinkedList;import java.util.Queue;public class J1700 &#123;    public int countStudents(int[] students, int[] sandwiches) &#123;        Queue&lt;Integer&gt; queue = new LinkedList&lt;&gt;();        for (int student : students) queue.offer(student);        for (int sandwich : sandwiches) &#123;            int r = 0;            while (!queue.isEmpty() &amp;&amp; queue.peek() != sandwich)&#123;                if (r == queue.size()) break;                queue.offer(queue.poll());                r++;            &#125;            if (!queue.isEmpty() &amp;&amp; queue.peek() != sandwich) return queue.size();            queue.poll();        &#125;        return queue.size();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>模拟</tag>
        <tag>队列</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 206. Reverse Linked List</title>
    <url>/2026/02/25/java/LeetCode-206-Reverse-Linked-List/</url>
    <content><![CDATA[问题描述给你单链表的头节点 head，请你反转链表，并返回反转后的链表。
解题思路本题使用递归方式反转链表，核心思想是利用函数调用栈天然的后进先出特性。
递归三部曲
终止条件：当链表为空 (head == null) 或只有一个节点 (head.next == null) 时，直接返回 head，因为空链表或单节点链表反转后仍是自身。

递推过程：递归调用 reverseList(head.next)，一直深入到链表的最后一个节点。当递归触底时，返回的 newHead 就是原链表的尾节点，也就是反转后链表的头节点。

回溯处理：当递归逐层返回时，假设当前节点为 head，其下一个节点 head.next 已经完成了它之后所有节点的反转。此时 head.next 指向的是反转后链表中的最后一个节点（即原链表中 head 之后的子链表反转后的尾节点）。我们需要做两件事：

将 head.next.next = head，让原本指向 head 的节点反过来指向 head
将 head.next = null，断开 head 对后续节点的引用，防止形成环



举例说明以链表 1 -&gt; 2 -&gt; 3 -&gt; 4 -&gt; 5 为例：

递归深入到节点 5，5.next == null，返回 5 作为 newHead
回溯到节点 4：4.next.next = 4（即 5.next = 4），4.next = null，得到 5 -&gt; 4
回溯到节点 3：3.next.next = 3（即 4.next = 3），3.next = null，得到 5 -&gt; 4 -&gt; 3
依此类推，最终得到 5 -&gt; 4 -&gt; 3 -&gt; 2 -&gt; 1

复杂度分析
时间复杂度：O(n)，需要遍历链表中的每个节点一次。
空间复杂度：O(n)，递归调用栈的深度为链表长度 n。

代码package code.J206;import datastructure.ListNode;public class J206 &#123;    public ListNode reverseList(ListNode head) &#123;        if (head == null || head.next == null) &#123;            return head;        &#125;        ListNode newHead = reverseList(head.next);        head.next.next = head;        head.next = null;        return newHead;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>链表</tag>
        <tag>递归</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 1929. Concatenation of Array</title>
    <url>/2026/02/14/java/LeetCode-1929-Concatenation-of-Array/</url>
    <content><![CDATA[题目描述给你一个长度为 n 的整数数组 nums。请你构建一个长度为 2n 的答案数组 ans，ans 由两个 nums 数组串联形成。ans[i] == nums[i] 且 ans[i + n] == nums[i]。
例如：nums = [1,2,1]，输出 [1,2,1,1,2,1]。
解释：数组 ans 由两个 [1,2,1] 串联组成。
解题思路核心思想这是一个非常直接的数组操作问题，本质上是将原数组复制两遍拼接到一起。具体做法是：

创建一个长度为 2n 的新数组 ans。
遍历原数组，将每个元素 nums[i] 分别放到 ans[i] 和 ans[i + n] 两个位置。
一次遍历即可完成。

步骤
ans = new int[nums.length * 2]
对于 i 从 0 到 nums.length - 1：
ans[i] = nums[i]
ans[i + nums.length] = nums[i]


返回 ans。

示例说明以 nums = [1,2,1] 为例：

n &#x3D; 3，ans 长度为 6
i&#x3D;0：ans[0]&#x3D;1, ans[3]&#x3D;1
i&#x3D;1：ans[1]&#x3D;2, ans[4]&#x3D;2
i&#x3D;2：ans[2]&#x3D;1, ans[5]&#x3D;1
结果：[1,2,1,1,2,1]

更简洁的写法也可以使用 System.arraycopy()：
int[] ans = new int[nums.length * 2];System.arraycopy(nums, 0, ans, 0, nums.length);System.arraycopy(nums, 0, ans, nums.length, nums.length);

复杂度分析
时间复杂度：O(n)，一次遍历原数组。
空间复杂度：O(n)（不计结果数组），结果数组长度为 2n。

代码package code.J1929;public class J1929 &#123;    public int[] getConcatenation(int[] nums) &#123;        int[] ans = new int[nums.length * 2];        for (int i = 0; i &lt; nums.length; i++) &#123;            ans[i] = nums[i];            ans[i + nums.length] = nums[i];        &#125;        return ans;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 232. Implement Queue using Stacks</title>
    <url>/2026/02/21/java/LeetCode-232-Implement-Queue-using-Stacks/</url>
    <content><![CDATA[问题描述请你仅使用两个栈实现先入先出队列。队列应当支持一般队列支持的所有操作（push、pop、peek、empty）。使用自定义的 MyQueue 类（位于 datastructure 包中），内部用两个栈实现队列。
解题思路栈是后进先出（LIFO）的数据结构，队列是先进先出（FIFO）的数据结构。要用栈模拟队列，关键在于两次入栈等于一次顺序反转。
双栈设计使用两个栈：

输入栈（stackIn）：专门负责接收 push 操作
输出栈（stackOut）：专门负责 pop 和 peek 操作

核心操作原理
push(x)：直接将元素压入 stackIn。时间复杂度 O(1)。

pop() &#x2F; peek()：

如果 stackOut 不为空，直接从 stackOut 弹出&#x2F;查看栈顶元素
如果 stackOut 为空，则将 stackIn 中的所有元素依次弹出并压入 stackOut。经过这次转移，stackIn 中的元素顺序被反转，stackOut 的栈顶恰好就是整个队列的队首元素


empty()：当两个栈都为空时，队列为空。


为什么这样做是正确的？假设依次 push 1, 2, 3：

stackIn（栈顶在右）：[1, 2, 3]
当需要 pop 时，将 stackIn 全部转移到 stackOut：
弹出 3，压入 stackOut → [3]
弹出 2，压入 stackOut → [3, 2]
弹出 1，压入 stackOut → [3, 2, 1]


此时 stackOut 栈顶是 1，正是最先入队的元素，pop 返回 1

关键点：只有当 stackOut 为空时才从 stackIn 转移元素，这保证了元素的 FIFO 顺序不被破坏。如果一边 pop 一边 push，新 push 的元素会暂存在 stackIn 中，等 stackOut 再次变空时才会被转移过来。
复杂度分析
push 时间复杂度：O(1)
pop&#x2F;peek 均摊时间复杂度：O(1)。虽然单次转移操作可能需要 O(n)，但每个元素最多被转移两次（一次从 stackIn 到 stackOut，一次从 stackOut 弹出），均摊下来每次操作 O(1)
空间复杂度：O(n)，两个栈共存储 n 个元素

代码package code.J232;import datastructure.MyQueue;public class J232 &#123;    MyQueue myQueue = new MyQueue();&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>栈</tag>
        <tag>队列</tag>
        <tag>设计</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 2073. Time Needed to Buy Tickets</title>
    <url>/2026/02/21/java/LeetCode-2073-Time-Needed-to-Buy-Tickets/</url>
    <content><![CDATA[题目描述有 n 个人前来排队买票，其中第 0 人站在队伍最前方，第 n-1 人站在队伍最后方。给定一个整数数组 tickets，其中 tickets[i] 表示第 i 个人想要购买的票数。每个人买票需要 1 秒。一个人一次只能买一张票，买完后如果还需要买更多票，就必须走到队尾重新排队。返回位于位置 k 的人完成买票所需的时间。
例如：tickets = [2,3,2]，k = 2，输出 6。
解释：排队的模拟过程为：

第 1 秒：第 0 人买一张，剩余 [1,3,2]，到队尾。
第 2 秒：第 1 人买一张，剩余 [1,2,2]，到队尾。
第 3 秒：第 2 人（k&#x3D;2）买一张，剩余 [1,2,1]，到队尾。
第 4 秒：第 0 人买一张，剩余 [0,2,1]，离队。
第 5 秒：第 1 人买一张，剩余 [0,1,1]，到队尾。
第 6 秒：第 2 人买最后一张，剩余 [0,1,0]，完成。所需时间为 6 秒。

解题思路模拟法（代码所用方法）直接按照题目描述的规则进行模拟。
步骤
初始化计数器 ans = 0，使用无限循环和取模运算模拟环形队列。
在循环中 index = i % tickets.length 获取当前买票人的下标：
如果该人还需要买票（tickets[index] &gt; 0）：
检查：如果当前买票人恰好是 k 且这是他最后一张票（tickets[k] == 1），那么这次买票就是他完成的时刻，返回 ++ans。
否则：ans++，tickets[index]--（买一张票）。




循环直到 k 位置的人完成买票。
返回 ans。

数学法（更优解法）实际上这题有 O(n) 的数学解法。对于位置 k 的人：

在他前面的人（i &lt; k）最多买 min(tickets[i], tickets[k]) 张票。
在他后面的人（i &gt; k）最多买 min(tickets[i], tickets[k] - 1) 张票（因为最后一轮在 k 完成之后才轮到他后面的人）。

总时间 &#x3D; 遍历所有人累加上述值即可。但模拟法更直观易懂。
示例说明以模拟法说明（见题目描述），此处不再赘述。
复杂度分析
时间复杂度：O(m * n)，其中 m 为最大票数，n 为人数。最坏情况下所有票数之和很大。
空间复杂度：O(1)，只使用常数额外空间。

代码package code.J2073;public class J2073 &#123;    public int timeRequiredToBuy(int[] tickets, int k) &#123;        int ans = 0;        for (int i = 0;true;i++)&#123;            int index = i%tickets.length;            if (tickets[index]&gt;0)&#123;                if (index == k &amp;&amp; tickets[k] == 1) return ++ans;                ans++;                tickets[index]--;            &#125;        &#125;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>模拟</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 316. Remove Duplicate Letters</title>
    <url>/2026/02/15/java/LeetCode-316-Remove-Duplicate-Letters/</url>
    <content><![CDATA[问题描述给你一个字符串 s，请你去除字符串中重复的字母，使得每个字母只出现一次。需保证返回结果的字典序最小（要求不能打乱其他字符的相对位置）。
解题思路本题的本质是：在保持原字符串中每个字符相对顺序的前提下，选出一个字典序最小的子序列，且每个字符恰好出现一次。这是经典的单调栈 + 贪心问题。
核心思路
统计每个字符的出现次数：用 count[26] 数组记录每个字符在剩余字符串中还剩多少个。这是判断”能否丢弃当前字符”的关键依据。

维护单调递增栈：遍历字符串，对于每个字符 c：

将 count[c] 减 1
如果 c 已经在栈中，跳过（确保每个字符只出现一次）
如果 c 不在栈中，检查栈顶字符 top：
如果 top &gt; c（栈顶字符字典序大于当前字符）且 count[top] &gt; 0（后续还会出现 top），则可以将 top 弹出。因为我们可以后面再添加 top，这样能让结果字典序更小
重复以上检查，直到栈为空或栈顶不能再弹出


将 c 压入栈


结果输出：栈中从底到顶就是最终结果的逆序，反转后得到答案。


举例说明以输入 s = &quot;bcabc&quot; 为例：



步骤
当前字符
count 状态
栈状态
说明



初始化
-
b:4, c:2, a:1
[]



i&#x3D;0
‘b’
b:3, c:2, a:1
[b]
栈空，直接压入


i&#x3D;1
‘c’
b:3, c:1, a:1
[b,c]
c &gt; b，压入


i&#x3D;2
‘a’
b:3, c:1, a:0
[a]
a &lt; c 且 c 后面还有(c:1&gt;0)，弹出 c；a &lt; b 且 b 后面还有(b:3&gt;0)，弹出 b；压入 a


i&#x3D;3
‘b’
b:2, c:1, a:0
[a,b]
b &gt; a，压入


i&#x3D;4
‘c’
b:2, c:0, a:0
[a,b,c]
c &gt; b，压入


结果：&quot;abc&quot;，字典序最小。
为什么用单调栈？贪心策略：对于相邻的两个字符，如果前一个字符字典序更大，且它之后还会再出现，那么丢弃它能让结果字典序更小。这天然适合用单调递增栈来维护。
复杂度分析
时间复杂度：O(n)，每个字符最多入栈出栈一次。
空间复杂度：O(1)，虽然使用了栈和计数数组，但它们的大小仅取决于字符集大小（26 个小写字母），是常数级别。

代码package code.J316;import java.util.*;public class J316 &#123;    public String removeDuplicateLetters(String s) &#123;        int[] count = new int[26];        for (int i = 0; i &lt; s.length(); i++) &#123;            count[s.charAt(i) - &#x27;a&#x27;]++;        &#125;        Deque&lt;Character&gt; stack = new ArrayDeque&lt;&gt;();        for (int i = 0; i &lt; s.length(); i++) &#123;            count[s.charAt(i) - &#x27;a&#x27;]--;            if (stack.contains(s.charAt(i))) &#123;                continue;            &#125;            while (!stack.isEmpty() &amp;&amp; stack.peek() &gt; s.charAt(i) &amp;&amp; count[stack.peek() - &#x27;a&#x27;] &gt; 0) &#123;                stack.pop();            &#125;            stack.push(s.charAt(i));        &#125;        StringBuilder sb = new StringBuilder();        while (!stack.isEmpty()) &#123;            sb.append(stack.pop());        &#125;        return sb.reverse().toString();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>单调栈</tag>
        <tag>字符串</tag>
        <tag>贪心</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 328. Odd Even Linked List</title>
    <url>/2026/02/25/java/LeetCode-328-Odd-Even-Linked-List/</url>
    <content><![CDATA[问题描述给定单链表的头节点 head，将所有索引为奇数的节点和索引为偶数的节点分别组合在一起，然后返回重新排序的列表。第一个节点索引为奇数，第二个节点为偶数，以此类推。
解题思路本题要求就地重新排列链表，将奇数位置（1, 3, 5, …）的节点放在前面，偶数位置（2, 4, 6, …）的节点放在后面，且保持各自的相对顺序。
双指针分离法使用两个指针 odd 和 even 分别追踪奇数和偶数链表的尾节点：

初始化：

odd 指向头节点（第 1 个节点，奇数位）
even 指向第二个节点（第 2 个节点，偶数位）
evenHead 保存偶数链表的头节点，用于最后拼接


交替跳转：在循环中，每次操作两个节点：

odd.next = even.next：将当前奇数节点的 next 跳过偶数节点，连接到下一个奇数节点
odd = odd.next：odd 指针移动到下一个奇数节点
even.next = odd.next：将当前偶数节点的 next 跳过新奇数节点，连接到下一个偶数节点
even = even.next：even 指针移动到下一个偶数节点


终止条件：当 even == null（偶数节点已用完）或 even.next == null（没有更多奇数节点）时停止。

拼接：将奇数链表的尾部 odd.next 连接到偶数链表的头部 evenHead，完成拼接。


举例说明以链表 1 -&gt; 2 -&gt; 3 -&gt; 4 -&gt; 5 为例：



步骤
odd 链表
even 链表
说明



初始
1 -&gt; 2 -&gt; 3 -&gt; 4 -&gt; 5
2 -&gt; 3 -&gt; 4 -&gt; 5
odd&#x3D;1, even&#x3D;2, evenHead&#x3D;2


第1轮
1 -&gt; 3 -&gt; 4 -&gt; 5
2 -&gt; 4 -&gt; 5
odd.next&#x3D;3, odd移向3; even.next&#x3D;4, even移向4


第2轮
1 -&gt; 3 -&gt; 5
2 -&gt; 4
odd.next&#x3D;5, odd移向5; even.next&#x3D;null, even移向null


终止
even&#x3D;null，停止




拼接
1 -&gt; 3 -&gt; 5 -&gt; 2 -&gt; 4

odd.next &#x3D; evenHead


为什么这样做是正确的？每次迭代中，我们分别将当前奇数节点连接到”隔一个”的下一个奇数节点，当前偶数节点连接到”隔一个”的下一个偶数节点。这样就同时维护了两条独立链表：

奇数链表包含所有奇数位置的节点：1 → 3 → 5 → …
偶数链表包含所有偶数位置的节点：2 → 4 → 6 → …

最后只要把奇数链表的末尾连接到偶数链表的头部即可。
复杂度分析
时间复杂度：O(n)，需要遍历整个链表一次。
空间复杂度：O(1)，只使用了有限的额外指针变量。

代码package code.J328;import datastructure.ListNode;public class J328 &#123;    public ListNode oddEvenList(ListNode head) &#123;        if (head == null || head.next == null) return head;        ListNode odd = head;        ListNode even = head.next;        ListNode evenHead = even;        while (even != null &amp;&amp; even.next != null) &#123;            odd.next = even.next;            odd = odd.next;            even.next = odd.next;            even = even.next;        &#125;        odd.next = evenHead;        return head;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 41. First Missing Positive</title>
    <url>/2026/02/28/java/LeetCode-41-First-Missing-Positive/</url>
    <content><![CDATA[题目描述给你一个未排序的整数数组 nums，请你找出其中没有出现的最小的正整数。要求时间复杂度 O(n) 且只使用常数级别额外空间。
解题思路这是一道经典的原地哈希题目。由于要求 O(n) 时间复杂度和 O(1) 额外空间，我们需要在原数组上进行操作。
核心思路是将数组视为一个”哈希表”，让每个正整数尽可能地放在它对应的索引位置上（即值 x 应该放在下标 x - 1 的位置）。如果数组中存在 1，那么 1 应该在下标 0 的位置；存在 2，那么 2 应该在下标 1 的位置，以此类推。
具体步骤分为三趟遍历：
第一趟：将数组中所有非正整数（≤ 0）替换为 len + 1（一个超出有效范围的值）。这是为了后续处理时，只关注正整数的范围，负数和其他非正整数不会干扰标记过程。
第二趟：使用数组的索引作为标记位。遍历数组，对于每个元素的绝对值 cur，如果它在有效范围 [1, len] 内，我们就将该值对应下标 cur - 1 处的元素标记为负数（取反）。这表示数字 cur 在数组中出现过。注意需要使用绝对值处理可能已被标记为负数的元素。
第三趟：遍历数组，找到第一个值大于 0 的下标 i，说明 i + 1 这个正整数没有出现过，即为答案。如果所有位置都被标记为负（即 1 到 len 都出现了），则返回 len + 1。
这种原地标记法的精妙之处在于，利用正负号作为布尔标记，不需要额外空间。
复杂度分析
时间复杂度：O(n)，遍历了三次数组，每次都是 O(n)。
空间复杂度：O(1)，只使用了几个变量，没有使用额外的数据结构。

代码package code.J41;import java.util.*;public class J41 &#123;    public int firstMissingPositive(int[] nums) &#123;        int len = nums.length;        for (int i = 0; i &lt; len; i++) &#123;            if (nums[i] &lt;= 0) &#123;                nums[i] = len + 1;            &#125;        &#125;        for (int num : nums) &#123;            int cur = Math.abs(num);            if (cur &lt;= len) &#123;                nums[cur - 1] = -Math.abs(nums[cur - 1]);            &#125;        &#125;        for (int i = 0; i &lt; len; i++) &#123;            if (nums[i] &gt; 0) &#123;                return i + 1;            &#125;        &#125;        return len + 1;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>原地哈希</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 448. Find All Numbers Disappeared in an Array</title>
    <url>/2026/02/14/java/LeetCode-448-Find-All-Numbers-Disappeared-in-an-Array/</url>
    <content><![CDATA[问题描述给你一个含 n 个整数的数组 nums，其中 nums[i] 在区间 [1, n] 内。请你找出所有在 [1, n] 范围内但没有出现在 nums 中的数字，并以数组的形式返回结果。
解题思路本题的核心难点在于要求 O(n) 时间复杂度和 O(1) 额外空间（返回列表不计入额外空间）。常规的哈希表做法需要 O(n) 空间，不符合要求。
原地哈希（标记法）元素的值范围是 [1, n]，恰好对应数组索引 [0, n-1]。利用这个特性，我们可以在原数组上进行”标记”：

第一次遍历 —— 标记出现过数字对应的索引：

对于每个元素 nums[i]，取其绝对值得到原值 val
计算索引 index = val - 1
将 nums[index] 变为负数（取反），表示”数字 val 已经出现过”
注意使用 Math.abs() 取绝对值，因为元素可能已经被之前的标记操作变成了负数


第二次遍历 —— 收集缺失的数字：

遍历数组，如果 nums[i] &gt; 0（仍为正数），说明数字 i + 1 没有出现过
将所有正数位置的索引 + 1 收集到结果列表中



举例说明以输入 nums = [4, 3, 2, 7, 8, 2, 3, 1]（n&#x3D;8）为例：
第一次遍历：



i
nums[i]
val(abs)
index
标记前 nums[index]
标记后 nums



0
4
4
3
7
[4,3,2,-7,8,2,3,1]


1
3
3
2
2
[4,3,-2,-7,8,2,3,1]


2
-2
2
1
3
[4,-3,-2,-7,8,2,3,1]


3
-7
7
6
3
[4,-3,-2,-7,8,2,-3,1]


4
8
8
7
1
[4,-3,-2,-7,8,2,-3,-1]


5
2
2
1
-3
已在步骤2标记，无变化


6
-3
3
2
-2
已标记，无变化


7
-1
1
0
4
[-4,-3,-2,-7,8,2,-3,-1]


第二次遍历：nums[4]=8&gt;0 和 nums[5]=2&gt;0，所以缺失数字为 4+1&#x3D;5 和 5+1&#x3D;6。
结果：[5, 6]
核心思想通过将元素值映射为数组索引，并用符号位（正负号）作为”标记”，在不消耗额外空间的前提下实现了哈希集合的功能。这是空间压缩的经典技巧。
复杂度分析
时间复杂度：O(n)，两次遍历数组。
空间复杂度：O(1)，仅使用常数额外空间（返回列表不计入）。

代码package code.J448;import java.util.ArrayList;import java.util.List;public class J448 &#123;    public List&lt;Integer&gt; findDisappearedNumbers(int[] nums) &#123;        for (int i = 0; i &lt; nums.length; i++) &#123;            int index = Math.abs(nums[i]) - 1;            if (nums[index] &gt; 0) &#123;                nums[index] = -nums[index];            &#125;        &#125;        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();        for (int i = 0; i &lt; nums.length; i++) &#123;            if (nums[i] &gt; 0) &#123;                res.add(i + 1);            &#125;        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>原地哈希</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 459. Repeated Substring Pattern</title>
    <url>/2026/02/25/java/LeetCode-459-Repeated-Substring-Pattern/</url>
    <content><![CDATA[问题描述给定一个非空的字符串 s，检查是否可以通过由它的一个子串重复多次构成。
解题思路如果字符串 s 可以由子串 p 重复 k 次构成，那么 s 的长度 n 必定是子串长度 len(p) 的整数倍，即 len(p) 是 n 的因数。
因数枚举法
找出所有可能的子串长度：

一个子串如果可以重复构成整个字符串，其长度必然是字符串总长度 n 的一个因数
遍历 1 到 n/2，收集所有 n 的因数（子串长度不可能超过 n/2，因为至少要重复两次）


逐个验证：

对于每个因数 factor（可能的子串长度）：
计算重复次数 count = n / factor
截取前 factor 个字符作为候选子串 sub
使用 sub.repeat(count) 生成重复字符串，与原始字符串 s 比较
如果相等，返回 true




返回结果：如果所有因数都无法匹配，返回 false。


举例说明以输入 s = &quot;abab&quot;（n&#x3D;4）为例：

因数有：[1, 2]
检查 factor&#x3D;1：sub = &quot;a&quot;，重复 4 次得 &quot;aaaa&quot; ≠ &quot;abab&quot;，不匹配
检查 factor&#x3D;2：sub = &quot;ab&quot;，重复 2 次得 &quot;abab&quot; &#x3D; &quot;abab&quot;，匹配，返回 true

以输入 s = &quot;abcabcabc&quot;（n&#x3D;9）为例：

因数有：[1, 3]（4 不是因数，跳过）
factor&#x3D;1：&quot;a&quot; 重复 9 次 ≠ 原串
factor&#x3D;3：&quot;abc&quot; 重复 3 次 &#x3D; 原串，返回 true

为什么枚举因数可行？如果子串长度不是总长度的因数，则无论如何重复都无法正好拼接成原字符串。因此只需要检查所有因数长度的子串即可覆盖所有可能的解，这是一种高效的剪枝策略。
复杂度分析
时间复杂度：O(n * d(n))，其中 d(n) 是 n 的因数个数。对于每个因数，需要进行一次字符串拼接和比较，耗时 O(n)。在最坏情况下 d(n) 约等于 O(√n)，总时间复杂度约为 O(n√n)。
空间复杂度：O(n)，需要存储拼接后的字符串进行比较。

代码package code.J459;import java.util.ArrayList;import java.util.List;public class J459 &#123;    private List&lt;Integer&gt; findFactor(int num) &#123;        List&lt;Integer&gt; factor = new ArrayList&lt;&gt;();        for (int i = 1; i &lt;= num / 2; i++) &#123;            if (num % i == 0) factor.add(i);        &#125;        return factor;    &#125;    public boolean repeatedSubstringPattern(String s) &#123;        int sLen = s.length();        List&lt;Integer&gt; factors = findFactor(sLen);        for (Integer factor : factors) &#123;            int count = sLen / factor;            String sub = s.substring(0, factor);            if (sub.repeat(count).equals(s)) return true;        &#125;        return false;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>数学</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 482. License Key Formatting</title>
    <url>/2026/02/21/java/LeetCode-482-License-Key-Formatting/</url>
    <content><![CDATA[问题描述给定一个许可密钥字符串 s，仅由字母、数字字符和破折号组成。字符串由 n 个破折号分成 n+1 组。给定一个整数 k，重新格式化字符串，使每一组恰好包含 k 个字符，第一组可以少于 k 个字符但至少包含一个字符。用破折号分隔组，所有小写字母转换为大写。
解题思路本题的要点在于理解分组规则：除了第一组外，其余每组恰好 k 个字符。这意味着我们需要先计算出总的有效字符数，然后确定第一组的大小。
算法步骤
去除破折号：

使用 s.split(&quot;-&quot;) 将原字符串按破折号分割
将所有非破折号部分拼接起来，得到所有有效字符的连续字符串


计算第一组长度：

设总有效字符数为 sumLength
第一组的长度 firstLen = sumLength % k
如果 firstLen == 0（恰好整除），则第一组也应取 k 个字符


按组格式化：

取前 firstLen 个字符作为第一组，转为大写
从 firstLen 位置开始，每次取 k 个字符作为一组，组间用 &quot;-&quot; 分隔，每组转为大写



举例说明示例 1：s = &quot;5F3Z-2e-9-w&quot;, k = 4

去除破折号后：&quot;5F3Z2e9w&quot;，共 8 个字符
firstLen = 8 % 4 = 0，所以 firstLen = 4（整除时每组都取 4 个）
第一组：&quot;5F3Z&quot; → &quot;5F3Z&quot;
第二组：&quot;2e9w&quot; → &quot;2E9W&quot;
结果：&quot;5F3Z-2E9W&quot;

示例 2：s = &quot;2-5g-3-J&quot;, k = 2

去除破折号后：&quot;25g3J&quot;，共 5 个字符
firstLen = 5 % 2 = 1
第一组：&quot;2&quot; → &quot;2&quot;
第二组：&quot;5g&quot; → &quot;5G&quot;
第三组：&quot;3J&quot; → &quot;3J&quot;
结果：&quot;2-5G-3J&quot;

关键点
第一组可以比其他组短，但不能为空
破折号在格式化后的字符串中仅作为分隔符出现，不能出现在开头或结尾
所有字母必须转换为大写

复杂度分析
时间复杂度：O(n)，需要遍历字符串一次，其中 n 为字符串长度。
空间复杂度：O(n)，需要额外空间存储处理后的字符串。

代码package code.J482;public class J482 &#123;    public String licenseKeyFormatting(String s, int k) &#123;        StringBuilder sb = new StringBuilder();        String[] strings = s.split(&quot;-&quot;);        if (strings.length &lt; 1) return &quot;&quot;;        int sumLength = 0;        for (String string : strings) &#123;            sb.append(string);            sumLength += string.length();        &#125;        int firstLen = sumLength % k;        if (firstLen == 0) firstLen = k;        StringBuilder result = new StringBuilder();        String str = sb.toString();        result.append(str.substring(0, firstLen).toUpperCase());        for (int i = firstLen; i &lt; str.length(); i += k) &#123;            if (!result.isEmpty()) result.append(&quot;-&quot;);            int end = Math.min(i + k, s.length());            result.append(str.substring(i, end).toUpperCase());        &#125;        return result.toString();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 485. Max Consecutive Ones</title>
    <url>/2026/02/14/java/LeetCode-485-Max-Consecutive-Ones/</url>
    <content><![CDATA[问题描述给定一个二进制数组 nums，计算其中最大连续 1 的个数。
解题思路本题是经典的遍历 + 计数器问题，思路直接但需要注意边界处理。
单指针遍历法使用两个变量：

pre_sum：当前连续 1 的计数
max_sum：全局最大连续 1 的个数

遍历数组：

遇到 1：pre_sum 加 1（或加 num 本身，因为 1 的值为 1）
遇到 0：
用 max_sum = Math.max(max_sum, pre_sum) 更新全局最大值
将 pre_sum 重置为 0（连续被打断）


遍历结束后，再次比较 max_sum 和 pre_sum，因为数组可能以 1 结尾而没有遇到 0 来触发更新

举例说明以输入 nums = [1, 1, 0, 1, 1, 1] 为例：



i
num
pre_sum
max_sum
说明



0
1
1
0
遇到 1，计数增加


1
1
2
0
遇到 1，计数增加


2
0
0
2
遇到 0，更新 max_sum&#x3D;2，重置 pre_sum


3
1
1
2
遇到 1，计数增加


4
1
2
2
遇到 1，计数增加


5
1
3
2
遇到 1，计数增加


结束
-
3
max(2,3)&#x3D;3
返回 3


最终结果为 3。
边界情况
全 1 数组：如 [1, 1, 1]，没有 0 触发更新，最后的 Math.max 处理了这种情况
全 0 数组：如 [0, 0]，pre_sum 始终为 0，max_sum 也为 0
单元素数组：[1] 返回 1，[0] 返回 0

复杂度分析
时间复杂度：O(n)，只需遍历数组一次。
空间复杂度：O(1)，只使用了两个整数变量。

代码package code.J485;public class J485 &#123;    public int findMaxConsecutiveOnes(int[] nums) &#123;        int pre_sum = 0;        int max_sum = 0;        for (int num : nums) &#123;            if (num == 0) &#123;                max_sum = Math.max(max_sum, pre_sum);                pre_sum = 0;            &#125; else &#123;                pre_sum += num;            &#125;        &#125;        return Math.max(max_sum, pre_sum);    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 520. Detect Capital</title>
    <url>/2026/02/21/java/LeetCode-520-Detect-Capital/</url>
    <content><![CDATA[问题描述我们定义，在以下情况时，单词的大写用法是正确的：

全部字母都是大写，比如 &quot;USA&quot;
单词中所有字母都不是大写，比如 &quot;leetcode&quot;
如果单词不只含有一个字母，只有首字母大写，比如 &quot;Google&quot;

给定一个字符串 word，判断其大写使用是否正确。
解题思路题目的三种合法情况可以归纳为三个独立的判断条件，满足其一即可：
辅助方法设计
isAllUppercase(str)：检查字符串中所有字符是否都是大写

遍历每个字符，如果发现小写字母则返回 false


isAllLowercase(str)：检查字符串中所有字符是否都是小写

遍历每个字符，如果发现大写字母则返回 false


isFirstUppercase(str)：检查是否首字母大写且其余字母小写

先检查首字母是否大写
如果是，从第二个字符开始遍历，发现大写字母则返回 false
如果首字母不是大写，直接返回 false



主逻辑return isAllLowercase(word) || isFirstUppercase(word) || isAllUppercase(word);

三个条件满足任意一个即返回 true。
举例说明


输入
isAllLowercase
isFirstUppercase
isAllUppercase
结果



&quot;USA&quot;
false
false
true
true


&quot;leetcode&quot;
true
false
false
true


&quot;Google&quot;
false
true
false
true


&quot;FlaG&quot;
false
false
false
false


&quot;g&quot;
true
false
false
true


&quot;G&quot;
false
false
true
true


边界情况
单字符单词：&quot;a&quot; 和 &quot;A&quot; 都算正确
空字符串：虽然题目通常会给出非空输入，但需要考虑防御性编程

复杂度分析
时间复杂度：O(n)，最多遍历字符串三次（实际只需一次，三个条件互斥时可提前返回）。
空间复杂度：O(1)，没有使用额外数据结构。

代码package code.J520;public class J520 &#123;    private boolean isAllUppercase(String str) &#123;        for (int i = 0; i &lt; str.length(); i++) &#123;            if (Character.isLowerCase(str.charAt(i))) return false;        &#125;        return true;    &#125;    private boolean isAllLowercase(String str) &#123;        for (int i = 0; i &lt; str.length(); i++) &#123;            if (Character.isUpperCase(str.charAt(i))) return false;        &#125;        return true;    &#125;    private boolean isFirstUppercase(String str) &#123;        if (Character.isUpperCase(str.charAt(0))) &#123;            for (int i = 1; i &lt; str.length(); i++) &#123;                if (Character.isUpperCase(str.charAt(i))) return false;            &#125;            return true;        &#125;        return false;    &#125;    public boolean detectCapitalUse(String word) &#123;        return isAllLowercase(word) || isFirstUppercase(word) || isAllUppercase(word);    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 636. Exclusive Time of Functions</title>
    <url>/2026/02/14/java/LeetCode-636-Exclusive-Time-of-Functions/</url>
    <content><![CDATA[题目描述有一个单线程 CPU 正在运行一个含有 n 道函数的程序。每道函数都有一个位于 [0, n-1] 的唯一标识符。
函数调用存储在一个调用栈上：当一个函数调用开始时，它的标识符将会推入栈中。而当一个函数调用结束时，它的标识符将会从栈中弹出。标识符位于栈顶的函数是当前正在执行的函数。
每当一个函数开始或者结束时，将会记录一条日志，包括函数标识符、是开始还是结束、以及相应的时间戳。给你一个由日志组成的列表 logs，请你返回每个函数的独占时间。
每条日志的格式为 &quot;function_id:start|end:timestamp&quot;，其中 timestamp 是一个非负整数，表示函数开始或结束的时间点。注意，同一个函数可能会被递归调用多次。
“独占时间” 是指该函数在 CPU 上执行的所有时间片段之和，不包括它调用其他函数所花费的时间。
解题思路本题的核心是模拟调用栈来跟踪当前正在执行的函数，并记录每个时间片段归谁所有。
关键点：时间片的含义时间戳表示某个时刻，函数从 timestamp 开始执行，或恰好在 timestamp 结束。举例来说：

0:start:2 表示函数 0 在时刻 2 开始
0:end:5 表示函数 0 在时刻 5 结束

那么函数 0 一共运行了 5 - 2 + 1 = 4 个单位时间（即时刻 2、3、4、5）。
算法步骤维护一个栈 stack、一个结果数组 res，和一个变量 prevTime 记录上一个事件发生的时间点。

遍历每一条日志，解析出 function_id、op（start 或 end）、time。

遇到 start 日志：

如果栈不为空，说明栈顶函数在 [prevTime, time-1] 这段时间内正在运行，将这 time - prevTime 的时间加到该函数的独占时间中。
将新函数入栈，并更新 prevTime = time。


遇到 end 日志：

弹出栈顶函数，该函数在当前栈顶期间运行了 [prevTime, time] 这段时间（注意包含结束时刻），所以独占时间加上 time - prevTime + 1。
更新 prevTime = time + 1，因为时刻 time + 1 开始由新的栈顶函数（如果有的话）占用。



为什么这样计算是正确的？关键在于 prevTime 的维护：每次事件发生时，我们结算上一个事件到当前事件之间 CPU 被谁占用。start 事件不包含当前时刻（当前时刻开始由新函数占用），而 end 事件包含当前时刻（函数恰好在此时刻结束），所以计算方式不同。
通过栈来追踪函数的嵌套关系，每次只是把时间片段分给栈顶函数——因为单线程 CPU 同一时刻只有一个函数在执行，而它一定是调用栈最顶端的那个函数。
复杂度分析
时间复杂度：O(m)，其中 m 是日志数量。我们只需要遍历一遍所有日志，每次日志的处理都是 O(1) 的栈操作。
空间复杂度：O(n)，其中 n 是函数数量。需要一个大小为 n 的结果数组来存储每个函数的独占时间，以及一个调用栈，最坏情况下所有函数都在调用栈中，空间为 O(n)。

代码package code.J636;import java.util.ArrayDeque;import java.util.Deque;import java.util.List;public class J636 &#123;    public int[] exclusiveTime(int n, List&lt;String&gt; logs) &#123;        Deque&lt;Integer&gt; deque = new ArrayDeque&lt;&gt;();        int[] res = new int[n];        int prevTime = 0;        for (String str : logs) &#123;            String[] split = str.split(&quot;:&quot;);            int function_id = Integer.parseInt(split[0]);            String op = split[1];            int time = Integer.parseInt(split[2]);            if (op.equals(&quot;start&quot;)) &#123;                if (!deque.isEmpty()) &#123;                    res[deque.peek()] += time - prevTime;                &#125;                deque.push(function_id);                prevTime = time;            &#125;            if (op.equals(&quot;end&quot;)) &#123;                res[deque.pop()] += time - prevTime + 1;                prevTime = time + 1;            &#125;        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>栈</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 523. Continuous Subarray Sum</title>
    <url>/2026/02/28/java/LeetCode-523-Continuous-Subarray-Sum/</url>
    <content><![CDATA[问题描述给你一个整数数组 nums 和一个整数 k，如果 nums 有一个好的子数组（长度至少为 2，且子数组元素总和为 k 的倍数），返回 true；否则返回 false。
解题思路本题是前缀和 + 同余定理的经典应用。
核心数学原理：同余定理若子数组 nums[i...j] 的和是 k 的倍数，则有：
(sum[j] - sum[i-1]) % k == 0
等价于：
sum[j] % k == sum[i-1] % k

这意味着：如果两个前缀和对 k 取余的余数相同，且它们对应的索引距离 ≥ 2，则它们之间的子数组和就是 k 的倍数。
算法步骤
初始化哈希表：将 (0, -1) 放入哈希表，表示前缀和为 0 时的索引为 -1。这是为了处理”从数组开头开始的子数组”的情况（如 [6, 6] 且 k=6）。

遍历数组，维护前缀和：

累加当前元素到 runningSum
计算 remainder = runningSum % k
注意处理 k == 0 的特例：直接取 runningSum 作为余数（即两个相同的前缀和才满足条件）
处理负数余数：如果 remainder &lt; 0，加上 k 使其变为正数（例如 Java 中 -1 % 6 = -1，需要变为 5）


检查哈希表：

如果当前余数已经在哈希表中出现过，说明存在两个位置的前缀和余数相同
检查两个位置的距离 i - map.get(remainder) 是否大于 1（确保子数组长度 ≥ 2）
如果距离 ≥ 2，返回 true
如果余数不在哈希表中，将 (remainder, i) 存入



举例说明示例 1：nums = [23, 2, 4, 6, 7], k = 6



i
num
runningSum
remainder (mod 6)
map
操作



-
-
-
-
{0: -1}
初始化


0
23
23
5
{0:-1, 5:0}
5 不在 map 中，存入


1
2
25
1
{0:-1, 5:0, 1:1}
1 不在 map 中，存入


2
4
29
5
-
5 在 map 中！检查距离：2 - 0 &#x3D; 2 &gt; 1，返回 true


子数组 [2, 4] 的和为 6，是 k&#x3D;6 的倍数，长度为 2。
示例 2：nums = [23, 2, 6, 4, 7], k = 6



i
runningSum
remainder
map
操作



-
-
-
{0: -1}
初始化


0
23
5
{0:-1, 5:0}
存入


1
25
1
{0:-1, 5:0, 1:1}
存入


2
31
1
-
1 在 map 中！距离：2-1&#x3D;1，≤1，不满足，不更新 map


3
35
5
-
5 在 map 中！距离：3-0&#x3D;3 &gt; 1，返回 true


子数组 [2, 6, 4] 的和为 12，是 k&#x3D;6 的倍数，长度为 3。
关键细节
为什么 map 只存余数首次出现的位置？ 因为我们希望子数组尽可能长，但更重要的是”存在至少一个”。存最早出现的位置能最大化距离，更容易满足长度 ≥ 2 的条件。如果余数重复出现，不需要更新 map。
为什么要有 (0, -1)？ 考虑 nums = [6], k = 6。runningSum = 6, remainder = 0。0 在 map 中（索引 -1），但 0 - (-1) = 1，不满足长度条件。而 nums = [6, 6], k = 6 时，第二个 6 的余数也是 0，1 - (-1) = 2 &gt; 1，返回 true。

复杂度分析
时间复杂度：O(n)，只需遍历数组一次，每次哈希表查找和插入都是 O(1)。
空间复杂度：O(min(n, k))，哈希表中最多存储 min(n, k) 个不同的余数。

代码package code.J523;import java.util.HashMap;import java.util.Map;public class J523 &#123;    public boolean checkSubarraySum(int[] nums, int k) &#123;        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();        map.put(0, -1);        int runningSum = 0;        for (int i = 0; i &lt; nums.length; i++) &#123;            runningSum += nums[i];            int remainder = (k == 0) ? runningSum : runningSum % k;            if (remainder &lt; 0) remainder += k;            if (map.containsKey(remainder)) &#123;                if (i - map.get(remainder) &gt; 1) return true;            &#125; else &#123;                map.put(remainder, i);            &#125;        &#125;        return false;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>哈希表</tag>
        <tag>数学</tag>
        <tag>前缀和</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 645. Set Mismatch</title>
    <url>/2026/02/14/java/LeetCode-645-Set-Mismatch/</url>
    <content><![CDATA[题目描述集合 s 包含从 1 到 n 的整数。不幸的是，因为数据错误，导致集合里面某一个数字复制成了集合里面的另外一个数字的值，导致集合丢失了一个数字并且有一个数字重复。
给定一个数组 nums 代表了该集合发生错误后的结果。找出重复出现的整数，再找到丢失的整数，将它们以数组的形式返回。
示例：输入 nums = [1,2,2,4]，输出 [2,3]。其中 2 是重复的数字，3 是丢失的数字。
解题思路本题本质上是在 1 到 n 这 n 个数字中，有一个数字出现了两次，有一个数字没有出现。我们需要找出这两个数字。
方法：计数数组因为数组中的数字都在 [1, n] 范围内，我们可以使用一个长度为 n 的辅助数组（或直接在原数组上标记）来统计每个数字出现的次数。

创建一个长度为 n 的数组 res，初始值全为 0。
遍历 nums，对于每个数字 num，将 res[num - 1] 的值加 1。
再次遍历 res（或遍历 1 到 n）：
如果 res[i] == 0，说明数字 i + 1 没有出现过，即丢失的数字。
如果 res[i] == 2，说明数字 i + 1 出现了两次，即重复的数字。


返回 [重复数字, 丢失数字]。

为什么这是正确的？每个数字理论上应该恰好出现一次。由于只有”一个重复、一个丢失”，统计完频次后，出现 0 次的必然是丢失的数字，出现 2 次的必然是重复的数字。其余数字出现次数均为 1。
这个方法的巧妙之处在于利用了数字范围和数组下标的对应关系，将数字本身映射到索引上作为计数槽位。
复杂度分析
时间复杂度：O(n)，其中 n 是数组长度。需要两次遍历，每次遍历都是 O(n)。
空间复杂度：O(n)，使用了一个大小为 n 的计数数组。


进阶：如果想达到 O(1) 额外空间，可以通过在原数组上用”取负数标记”或”异或运算”来实现，但上述计数法最为直观易懂。

代码package code.J645;public class J645 &#123;    public int[] findErrorNums(int[] nums) &#123;        int errorNum = 0, notFoundNum = 0;        int[] res = new int[nums.length];        for (int num : nums) &#123;            res[num - 1]++;        &#125;        for (int i = 0; i &lt; nums.length; i++) &#123;            if (res[i] == 0) notFoundNum = i + 1;            if (res[i] == 2) errorNum = i + 1;        &#125;        return new int[]&#123;errorNum, notFoundNum&#125;;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>哈希表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 66. Plus One</title>
    <url>/2026/02/15/java/LeetCode-66-Plus-One/</url>
    <content><![CDATA[题目描述给定一个由整数组成的非空数组所表示的非负整数，在该数的基础上加一。最高位数字存放在数组的首位，数组中每个元素只存储单个数字。你可以假设除了整数 0 之外，这个整数不会以零开头。
解题思路这道题模拟的是整数加一的过程。由于数组中每个元素只存储单个数字，我们需要从最低位（数组末尾）开始处理进位。
核心思路如下：
从数组的最低位（digits.length - 1）向最高位（下标 0）遍历：

如果当前位小于 9，直接加一并返回结果（因为不会有进位，后面的高位无需处理）。
如果当前位等于 9，将其置为 0，并继续向前一位处理进位。

如果遍历完整个数组后仍然没有返回（即所有位都是 9，例如 999），说明需要进位到更高的位。此时创建一个比原数组长一位的新数组，将首位设为 1，其余位默认为 0，即得到了结果（例如 1000）。
这个解法的巧妙之处在于：

利用”遇到小于 9 直接返回”的提前终止策略，避免了不必要的遍历
只有在全为 9 的极端情况下才需要扩容数组

复杂度分析
时间复杂度：O(n)，其中 n 是数组长度。最坏情况下需要遍历整个数组（所有位都是 9）。
空间复杂度：O(1)，除了最坏情况下需要创建新数组（全 9 时 O(n)），其余情况在原数组上操作。

代码package code.J66;public class J66 &#123;    public int[] plusOne(int[] digits) &#123;        for (int i = digits.length - 1; i &gt;= 0; i--) &#123;            if (digits[i] &lt; 9) &#123;                digits[i]++;                return digits;            &#125;            digits[i] = 0;        &#125;        digits = new int[digits.length + 1];        digits[0] = 1;        return digits;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>数学</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 686. Repeated String Match</title>
    <url>/2026/02/25/java/LeetCode-686-Repeated-String-Match/</url>
    <content><![CDATA[题目描述给定两个字符串 a 和 b，寻找重复叠加字符串 a 的最小次数，使得字符串 b 成为叠加后的字符串 a 的子串，如果不存在则返回 -1。
例如：

a = &quot;abcd&quot;, b = &quot;cdabcdab&quot; → 输出 3，因为 &quot;abcdabcdabcd&quot; 包含 &quot;cdabcdab&quot;。
a = &quot;a&quot;, b = &quot;aa&quot; → 输出 2。

解题思路题目要求找到最小的整数 k，使得 b 是 a 重复 k 次得到的字符串的子串。
核心分析我们需要确定重复次数的上界。假设 a 的长度为 lenA，b 的长度为 lenB。
要让 b 成为子串，叠加后的字符串长度至少要为 lenB。最朴素的想法是：重复 ceil(lenB / lenA) 次。但考虑到 b 可能跨越 a 的边界（即 b 从前一个 a 的尾部开始，延伸到下一个 a 的前部），我们需要在首尾各多加上一个完整的 a。
因此，最多需要重复 (lenB / lenA) + 2 次（或者更精确地说，ceil(lenB / lenA) + 1 次）。
算法步骤
计算 len = (lenB + lenA) / lenA + 1，即需要尝试的最大重复次数（上限）。
使用 StringBuilder 逐步拼接 a，每拼接一次就在当前累积字符串中判断是否包含 b。
如果在第 i 次拼接后找到了 b，直接返回 i（即拼接次数）。
如果遍历完所有可能的拼接次数仍未找到，返回 -1。

边界情况
如果 b 为空字符串，根据题意返回 0。
当 a 的长度大于等于 b 时，只需重复 1 到 2 次即可覆盖所有可能，代码中的循环上界保证了这一点。

为什么循环次数这么取？设叠加后的字符串为 S = a + a + ... + a（共 k 次）。如果 b 是 S 的子串，那么 b 的起始位置一定在前 lenA 个字符中（或者说 b 最多跨越 k 个 a）。换句话说，如果重复 ceil(lenB / lenA) + 1 次都找不到，说明再重复更多次也不可能找到——因为再增加一个 a 只是在首尾增加了相同的内容，不会新产生 b 作为子串的可能性。
复杂度分析
时间复杂度：O(n * m)，其中 n = lenA，m = lenB。最坏情况下需要拼接 m/n + 1 次，每次 contains 调用在 Java 中的复杂度为 O(lenA * k * lenB) 的朴素匹配。总体上约为 O(lenA * lenB)。
空间复杂度：O(lenA * k)，StringBuilder 存储了叠加后的字符串，最大长度约为 lenA * (lenB / lenA + 1) ≈ lenB + lenA。

代码package code.J686;public class J686 &#123;    public int repeatedStringMatch(String a, String b) &#123;        int lenA = a.length();        int lenB = b.length();        if(b.isEmpty()) return 0;        int len = (lenB + lenA) / lenA + 1;        StringBuilder sumA = new StringBuilder();        for (int i = 0; i &lt;= len; i++) &#123;            if (sumA.toString().contains(b)) return i;            sumA.append(a);        &#125;        return -1;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 796. Rotate String</title>
    <url>/2026/02/25/java/LeetCode-796-Rotate-String/</url>
    <content><![CDATA[题目描述给定两个字符串 s 和 goal，如果在若干次旋转操作之后，s 能变成 goal，那么返回 true。
s 的旋转操作就是将 s 最左边的字符移动到最右边。
例如：

s = &quot;abcde&quot;, goal = &quot;cdeab&quot; → 输出 true
s = &quot;abcde&quot;, goal = &quot;abced&quot; → 输出 false

解题思路本题有一个非常巧妙的技巧。
核心观察如果我们把 s 和自己拼接起来得到 s + s，那么 s 的所有旋转结果都一定是 s + s 的一个子串。
例如 s = &quot;abcde&quot;：

s + s = &quot;abcdeabcde&quot;
旋转 1 次：&quot;bcdea&quot; → 是 s + s 从下标 1 开始的子串
旋转 2 次：&quot;cdeab&quot; → 是 s + s 从下标 2 开始的子串
旋转 3 次：&quot;deabc&quot; → 是 s + s 从下标 3 开始的子串
旋转 4 次：&quot;eabcd&quot; → 是 s + s 从下标 4 开始的子串

所以，s 能否通过旋转变成 goal，等价于：

s 和 goal 长度相同（否则不可能通过旋转得到）；
goal 是 s + s 的子串。

算法步骤
先判断 s.length() 是否等于 goal.length()，不相等直接返回 false。
将 s 拼接自身得到 s + s，使用 contains 方法检查是否包含 goal。
返回 contains 的结果。

正确性证明设 s 的长度为 n，对 s 进行 k 次旋转（0 ≤ k &lt; n）后得到的结果，等价于将 s 的前 k 个字符移动到末尾。这一结果恰好等于 s + s 中从下标 k 开始、长度为 n 的子串。
反过来，如果 goal 是 s + s 的子串，那么 goal 在 s + s 中的起始下标 i 一定满足 0 ≤ i &lt; n（因为 goal 的长度也是 n），这意味着对 s 进行 i 次旋转后即可得到 goal。
复杂度分析
时间复杂度：O(n)，其中 n 是字符串长度。Java 中 contains 的内部实现（如 indexOf）在最坏情况下为 O(n)。
空间复杂度：O(n)，拼接 s + s 需要一个长度为 2n 的字符串。

代码package code.J796;public class J796 &#123;    public boolean rotateString(String s, String goal) &#123;        if (s.length() != goal.length()) return false;        return (s + s).contains(goal);    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 739. Daily Temperatures</title>
    <url>/2026/02/14/java/LeetCode-739-Daily-Temperatures/</url>
    <content><![CDATA[题目描述给定一个整数数组 temperatures，表示每天的温度，返回一个数组 answer，其中 answer[i] 是指对于第 i 天，下一个更高温度出现在几天后。如果气温在这之后都不会升高，请在该位置用 0 来代替。
例如：temperatures = [73,74,75,71,69,72,76,73]，输出 [1,1,4,2,1,1,0,0]。
解释：第 0 天温度 73，下一天（第 1 天）温度 74 &gt; 73，所以 answer[0] = 1；第 2 天温度 75，4 天后（第 6 天）温度 76 &gt; 75，所以 answer[2] = 4。
解题思路本题是经典的单调栈问题——更具体地说，是单调递减栈的应用，用来寻找每个元素右侧第一个比它大的元素的距离。
暴力解法对于每一天，向后扫描直到找到更高的温度，时间复杂度为 O(n^2)。当 n = 10^5 时会超时。
单调栈解法核心思想：维护一个从栈底到栈顶递减（指温度值）的栈，栈中存储的是下标而非温度值。

遍历温度数组，对于每一天 i：
当栈不为空，且当前温度 temperatures[i] 大于栈顶下标对应的温度 temperatures[stack.peek()] 时：
说明栈顶这一天终于等到了更高的温度！
弹出栈顶下标 index，计算 answer[index] = i - index。
继续检查新的栈顶，直到不满足条件为止。


将当前下标 i 入栈。

为什么单调栈可以解决问题？以 [73,74,75,71,69,72,76,73] 为例，在遍历到第 5 天（温度 72）之前：

栈内存储的是 [75, 71, 69] 的下标（即 [2, 3, 4]），温度从底到顶是递减的。

当 i&#x3D;5（温度 72）到来时：

72 &gt; 69 → 弹出 69（下标 4），answer[4] &#x3D; 5-4 &#x3D; 1
72 &gt; 71 → 弹出 71（下标 3），answer[3] &#x3D; 5-3 &#x3D; 2
72 &lt; 75 → 停止，将 72（下标 5）入栈

单调栈保证了每个元素最多进栈一次、出栈一次，从而将时间复杂度从暴力的 O(n²) 降为 O(n)。
核心要点
存储下标而非值，是为了方便计算距离 i - index。
当栈中仍有剩余元素时，说明这些天之后没有更高的温度，answer 数组的对应位置保持默认值 0 即可。

复杂度分析
时间复杂度：O(n)，每个下标最多入栈一次、出栈一次，所有操作均摊下来是 O(1)。
空间复杂度：O(n)，最坏情况下栈中可能存储所有下标（单调递减序列）。

代码package code.J739;import java.util.ArrayDeque;import java.util.Deque;public class J739 &#123;    public int[] dailyTemperatures(int[] temperatures) &#123;        int[] res = new int[temperatures.length];        Deque&lt;Integer&gt; stack = new ArrayDeque&lt;&gt;();        for (int i = 0; i &lt; temperatures.length; i++) &#123;            while (!stack.isEmpty() &amp;&amp; temperatures[stack.peek()] &lt; temperatures[i]) &#123;                int index = stack.pop();                res[index] = i - index;            &#125;            stack.push(i);        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>单调栈</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 83. Remove Duplicates from Sorted List</title>
    <url>/2026/02/25/java/LeetCode-83-Remove-Duplicates-from-Sorted-List/</url>
    <content><![CDATA[题目描述给定一个已排序的链表的头 head，删除所有重复的元素，使每个元素只出现一次。返回已排序的链表。
解题思路本题要求删除排序链表中的重复元素。由于链表已经排序，重复元素必然相邻，因此只需要遍历链表，比较当前节点和下一个节点的值是否相同。
核心思路：

首先处理特殊情况：如果链表为空（head == null），直接返回 null。
保存头节点指针 res，用于最终返回结果。
遍历链表，当 head.next 不为空时：
如果当前节点值等于下一个节点值（head.val == head.next.val），说明找到了重复元素，将当前节点的 next 指针跳过下一个节点，直接指向下下个节点（head.next = head.next.next）。注意这里使用 continue 而不是移动 head，因为可能需要连续删除多个重复元素（例如 1 → 1 → 1 → 2）。
如果值不相同，正常移动指针到下一个节点（head = head.next）。



这种做法的关键是：当删除一个重复节点后不立即移动指针，这是因为被跳过的那个节点后面的新节点可能仍然与当前节点值相同，需要继续判断。
复杂度分析
时间复杂度：O(n)，其中 n 是链表节点数。每个节点最多被访问一次。
空间复杂度：O(1)，只使用了常数级别的额外空间（几个指针变量）。

代码package code.J83;import datastructure.ListNode;public class J83 &#123;    public ListNode deleteDuplicates(ListNode head) &#123;        ListNode res = head;        if (head == null)&#123;            return null;        &#125;        while (head.next != null)&#123;            if (head.val == head.next.val) &#123;                head.next = head.next.next;                continue;            &#125;            head = head.next;        &#125;        return res;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 831. Masking Personal Information</title>
    <url>/2026/02/21/java/LeetCode-831-Masking-Personal-Information/</url>
    <content><![CDATA[题目描述给你一条个人信息字符串 s，可能表示一个邮箱地址，也可能表示一串电话号码。按照以下规则对个人信息进行隐藏&#x2F;加密处理：
邮箱地址规则：

所有字母转为小写。
保留邮箱名的第一个字符和 @ 之前最后一个字符，中间用 &quot;*****&quot; 替换。
域名保持不变。

举例：&quot;LeetCode@LeetCode.com&quot; → &quot;l*****e@leetcode.com&quot;
电话号码规则：

处理前 10-13 位数字的电话号码。
手机号码格式为 &quot;***-***-XXXX&quot;（10 位）。
带有国家代码时，国家代码的每一位用 &quot;+&quot; 开头，每一位用 &quot;*&quot; 代替（除最后一位外），如 11 位为 &quot;+*-***-***-XXXX&quot;，12 位为 &quot;+**-***-***-XXXX&quot;，13 位为 &quot;+***-***-***-XXXX&quot;。
输入可能包含 +、-、(、) 和空格等分隔符，需要先去除再做处理。

解题思路本题的核心是区分输入是邮箱还是电话号码，然后分别处理。
第一步：识别类型使用正则表达式区分输入类型：

邮箱：包含 @ 符号，特点是以字母开头，格式为 name@domain.tld。不使用正则时，可以直接判断字符串是否包含 @；题目代码中使用正则 ^[a-zA-Z]+@[a-zA-Z]+\\.[a-zA-Z]+$ 来做更严格的匹配。
电话号码：当不是邮箱时，匹配电话号码格式——包含 10-13 位数字，可能夹杂各种分隔符。正则 ^(?=(?:\\D*\\d){10,13}\\D*$)[+()\\s-]*\\d[\\d+()\\s-]*$ 用于验证是否为有效电话号码。

第二步：邮箱处理（maskEmail）
将整个字符串转为小写。
用 @ 分割得到用户名 name 和域名 domain。
取用户名的第一个字符 + &quot;*****&quot; + 用户名的最后一个字符作为隐藏后的用户名。
拼接结果：隐藏后的用户名 + &quot;@&quot; + 域名。

第三步：电话号码处理（maskPhone）
使用 replaceAll 去除所有非数字字符（+、-、(、)、空格）。
提取最后 4 位数字作为 maskPhone（保留部分）。
根据电话号码的总长度（10、11、12、13 位）选择对应的掩码格式：
10 位（如美国）：&quot;***-***-&quot; + 后四位
11 位（如中国+86）：&quot;+\*-***-***-&quot; + 后四位（一位国家代码，用 * 隐藏）
12 位：&quot;+\*\*-***-***-&quot; + 后四位（两位国家代码隐藏）
13 位：&quot;+\*\*\*-***-***-&quot; + 后四位（三位国家代码隐藏）



复杂度分析
时间复杂度：O(n)，其中 n 是输入字符串长度。正则匹配、字符串替换和拼接操作都只需线性扫描。
空间复杂度：O(n)，需要存储处理后的字符串。

代码package code.J831;import java.util.Locale;public class J831 &#123;    String emailSchema = &quot;^[a-zA-Z]+@[a-zA-Z]+\\.[a-zA-Z]+$&quot;;    String phoneSchema = &quot;^(?=(?:\\D*\\d)&#123;10,13&#125;\\D*$)[+()\\s-]*\\d[\\d+()\\s-]*$&quot;;    public String maskPII(String s) &#123;        if (s.matches(emailSchema)) return maskEmail(s);        if (s.matches(phoneSchema)) return maskPhone(s);        return s;    &#125;    private String maskPhone(String s) &#123;        String phone = s.replaceAll(&quot;[+\\-()\\s]&quot;, &quot;&quot;);        int phoneLen = phone.length();        String maskPhone = phone.substring(phoneLen - 4, phoneLen);        return switch (phoneLen) &#123;            case 10 -&gt; &quot;***-***-&quot; + maskPhone;            case 11 -&gt; &quot;+*-***-***-&quot; + maskPhone;            case 12 -&gt; &quot;+**-***-***-&quot; + maskPhone;            case 13 -&gt; &quot;+***-***-***-&quot; + maskPhone;            default -&gt; s;        &#125;;    &#125;    private String maskEmail(String s) &#123;        StringBuilder sb = new StringBuilder();        String str = s.toLowerCase();        String name = str.split(&quot;@&quot;)[0];        String maskName = name.charAt(0) + &quot;*****&quot; + name.charAt(name.length() - 1);        sb.append(maskName);        sb.append(&quot;@&quot;);        sb.append(str.split(&quot;@&quot;)[1]);        return sb.toString();    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>字符串</tag>
        <tag>正则表达式</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 84. Largest Rectangle in Histogram</title>
    <url>/2026/02/14/java/LeetCode-84-Largest-Rectangle-in-Histogram/</url>
    <content><![CDATA[题目描述给定 n 个非负整数，用来表示柱状图中各个柱子的高度。每个柱子彼此相邻，且宽度为 1。求在该柱状图中，能够勾勒出来的矩形的最大面积。
解题思路本题要求计算柱状图中能勾勒出的最大矩形面积，是单调栈的经典应用。
核心思路是：对于每一根柱子，如果我们以它的高度作为矩形的高，那么我们需要找到它左右两边第一个比它矮的柱子，这两个柱子之间的宽度就是该矩形可以扩展的最大宽度。
单调栈解法：

预处理：在原数组的首尾各添加一个高度为 0 的哨兵柱子。这样做的好处是：

头部哨兵确保栈永远不会为空（stack.peek() 始终有效）
尾部哨兵确保遍历结束后，栈中所有柱子都会出栈计算面积


维护单调递增栈：栈中存储的是下标，对应的高度值单调递增。遍历每个柱子 i：

当当前柱子的高度小于栈顶柱子的高度时，说明栈顶柱子的”右边界”已经找到了。弹出栈顶，以该柱子的高度作为矩形高，宽度为 i - left - 1（其中 left 是弹出后新的栈顶），计算面积并更新最大值。
重复上述过程，直到栈顶柱子高度不大于当前柱子，然后将当前柱子下标入栈。


单调栈的本质：栈中始终保持着高度递增的柱子序列。当遇到一个更矮的柱子时，它意味着栈中所有比它高的柱子都无法继续向右扩展了，这些柱子可以依次出栈并计算以它们为高度的最大矩形面积。


每个柱子只会入栈和出栈一次，因此总体的时间复杂度为 O(n)。
复杂度分析
时间复杂度：O(n)，每个元素最多入栈一次，出栈一次。
空间复杂度：O(n)，单调栈和扩展后的数组都需要 O(n) 的空间。

代码package code.J84;import java.util.ArrayDeque;import java.util.Arrays;import java.util.Deque;public class J84 &#123;    public int largestRectangleArea(int[] heights) &#123;        int max = 0;        Deque&lt;Integer&gt; stack = new ArrayDeque&lt;&gt;();        int[] newHeights = new int[heights.length + 2];        System.arraycopy(heights, 0, newHeights, 1, heights.length);        heights = newHeights;        stack.push(0);        for (int i = 1; i &lt; heights.length; i++) &#123;            while (!stack.isEmpty() &amp;&amp; heights[stack.peek()] &gt; heights[i]) &#123;                int height = heights[stack.pop()];                int left = stack.peek();                int width = i - left - 1;                max = Math.max(max, height * width);            &#125;            stack.push(i);        &#125;        return max;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>单调栈</tag>
      </tags>
  </entry>
  <entry>
    <title>LeetCode 941. Valid Mountain Array</title>
    <url>/2026/02/15/java/LeetCode-941-Valid-Mountain-Array/</url>
    <content><![CDATA[题目描述给定一个整数数组 arr，如果它是有效的山脉数组就返回 true，否则返回 false。
有效山脉数组的定义：

arr.length &gt;= 3
存在某个下标 i（0 &lt; i &lt; arr.length - 1）使得：
arr[0] &lt; arr[1] &lt; ... &lt; arr[i-1] &lt; arr[i]
arr[i] &gt; arr[i+1] &gt; ... &gt; arr[arr.length - 1]



即数组先严格递增，到某个峰值后严格递减。峰值不能在数组的首尾位置。
例如：

arr = [0,3,2,1] → true（峰值 3 在下标 1）
arr = [3,5,5] → false（有相等元素，不是严格递增&#x2F;递减）
arr = [0,1,2,3] → false（只有递增，没有递减）

解题思路本题可以用线性扫描（类似双指针的思想）一次遍历解决。
算法步骤
如果数组长度小于 3，直接返回 false（不满足山峰的最短长度要求）。

第一阶段：爬坡（递增段扫描）

从下标 0 开始向右遍历，只要 arr[i] &lt; arr[i+1] 就继续向前。
当不再满足严格递增时停下，此时 i 即为候选峰值位置。


第二阶段：下坡（递减段扫描）

从峰值位置 i 继续向右遍历，只要 arr[j] &gt; arr[j+1] 就继续向前。
如果在这个过程中发现 arr[j] &lt;= arr[j+1]（即不满足严格递减），说明不是山峰，直接返回 false。


边界检查：

峰值位置 i 不能是 0（说明数组一开始就在递减，没有上升段）。
峰值位置 i 不能是 arr.length - 1（说明数组从头到尾都在递增，没有下降段）。


如果以上全部通过，返回 true。


为什么这是正确的？山脉数组要求恰好一个峰值，峰值左侧严格递增、右侧严格递减。我们通过一次线性扫描依次验证这两个单调性质，并在最后检查峰值是否在有效位置（不在两端），即可完全判定。
这种”先找坡度变化点，再验证两侧”的方法等价于使用两个指针分别从两侧向中间爬坡的经典解法，但只需一次遍历，更为简洁。
复杂度分析
时间复杂度：O(n)，其中 n 是数组长度。每个元素至多被访问一次。
空间复杂度：O(1)，只使用了常数级别的额外空间。

代码package code.J941;public class J941 &#123;    public boolean validMountainArray(int[] arr) &#123;        if (arr.length &lt; 2) return false;        int i = 0;        for (; i &lt; arr.length - 1; i++) &#123;            if (arr[i] &gt;= arr[i + 1]) break;        &#125;        for (int j = i; j &lt; arr.length - 1; j++) &#123;            if (arr[j] &lt;= arr[j + 1]) return false;        &#125;        return i != 0 &amp;&amp; i != arr.length - 1;    &#125;&#125;
]]></content>
      <categories>
        <category>leetcode</category>
      </categories>
      <tags>
        <tag>数组</tag>
        <tag>Java</tag>
        <tag>双指针</tag>
      </tags>
  </entry>
  <entry>
    <title>通过 archinstall 安装 Arch Linux</title>
    <url>/2026/06/17/linux/01-archinstall%E5%AE%89%E8%A3%85Arch-Linux/</url>
    <content><![CDATA[通过 archinstall 安装 Arch Linux
适用读者：对 Linux 有基本了解、想体验 Arch Linux 的开发者，以及希望了解 Arch 安装全过程的 Linux 新手。


前言：为什么选择 Arch Linux？Arch Linux 以”滚动更新”著称——软件包永远是最新的，不存在大版本跨越的升级阵痛。它不预装任何多余的东西，你安装什么就有什么，系统完全由你掌控。代价是：安装门槛比 Ubuntu、Fedora 等发行版要高一些。
好在官方提供了 archinstall 这个交互式安装脚本，极大降低了安装难度，同时不失对安装过程的精确控制。

一、准备工作1.1 下载 ISO 镜像前往 Arch Linux 官网下载最新 ISO：
https://archlinux.org/download/

国内推荐用镜像站加速下载：

清华大学镜像站：https://mirrors.tuna.tsinghua.edu.cn/archlinux/iso/latest/
中科大镜像站：https://mirrors.ustc.edu.cn/archlinux/iso/latest/

下载文件名形如 archlinux-2025.xx.01-x86_64.iso。
1.2 制作启动 U 盘Windows 系统推荐用 Rufus：

插入 U 盘（建议 ≥ 8GB，U 盘内容会被清空）
打开 Rufus，”设备”选择你的 U 盘
“启动类型”点击”选择”，找到下载好的 ISO 文件
分区类型选 GPT，目标系统选 UEFI（非 CSM）
点击”开始”，弹窗选”以 ISO 镜像模式写入”，等待完成

Linux 系统直接用 dd 命令：
# 先确认 U 盘设备名，比如 /dev/sdb，千万别搞错lsblk# 写入（/dev/sdX 替换为你的 U 盘）sudo dd if=archlinux-*.iso of=/dev/sdX bs=4M status=progress oflag=sync

1.3 BIOS&#x2F;UEFI 设置重启电脑，开机时按 F2、DEL 或 F12（取决于主板厂商）进入 BIOS：

关闭 Secure Boot（安全启动）——Arch 默认 ISO 不支持 Secure Boot
将 U 盘设为第一启动项
确认启动模式为 UEFI（而非 Legacy&#x2F;BIOS）

保存设置并重启，电脑会从 U 盘启动。

二、进入 Live 环境成功从 U 盘启动后，你会看到 Arch Linux 启动菜单，选择第一项回车，稍等片刻进入命令行界面，提示符变为：
root@archiso ~ #

这就是 Arch 的”Live 环境”——一个运行在内存里的临时系统，用来执行安装操作。
2.1 连接网络有线网络：通常插上网线即可自动获取 IP，用以下命令确认：
ping -c 3 archlinux.org

Wi-Fi 网络：使用内置的 iwctl 工具：
iwctl# 进入 iwctl 交互模式后：device list                        # 查看无线网卡名称，比如 wlan0station wlan0 scan                 # 扫描周边 Wi-Fistation wlan0 get-networks         # 列出扫描结果station wlan0 connect &quot;你的WiFi名&quot; # 连接，然后输入密码exit                               # 退出 iwctl

连上后同样用 ping 验证：
ping -c 3 archlinux.org

2.2 更新系统时钟网络时间同步，避免后续软件包签名验证失败：
timedatectl set-ntp true

2.3 切换国内镜像源（可选但强烈推荐）默认镜像源在国外，速度可能很慢。编辑镜像列表，将国内源放到最前面：
# 用 reflector 自动选择最快的国内镜像reflector --country China --age 12 --protocol https --sort rate --save /etc/pacman.d/mirrorlist

或者手动编辑：
nano /etc/pacman.d/mirrorlist

在文件最顶部加入（保留其他内容）：
Server = https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$archServer = https://mirrors.ustc.edu.cn/archlinux/$repo/os/$arch


三、运行 archinstall准备就绪，启动安装脚本：
archinstall

你会看到一个蓝色的交互式菜单界面，用方向键和回车操作。
下面逐项讲解每个配置选项的含义和推荐设置。

3.1 Archinstall language（脚本语言）这是安装脚本界面的语言，不影响安装后系统的语言。
推荐选 English（默认），原因是中文翻译可能不完整，英文选项与文档一致。

3.2 Mirrors &#x2F; Mirror region（镜像源地区）选择软件包下载的镜像源地区。
选择 China，archinstall 会自动从国内镜像站下载，速度快很多。

3.3 Locales（本地化）设置系统的语言和编码。

Locale language：推荐选 zh_CN.UTF-8（简体中文），这样安装完系统界面可以是中文
Locale encoding：保持 UTF-8


3.4 Disk configuration（磁盘分区）
⚠️ 警告：这一步会格式化磁盘，操作前务必确认目标磁盘，并备份重要数据。

选择 Use a best-effort default partition layout（使用最佳默认分区方案），archinstall 会自动帮你分区：

选中你要安装的磁盘（比如 /dev/sda 或 /dev/nvme0n1）
文件系统类型推荐选 ext4（稳定成熟）或 btrfs（支持快照，适合喜欢折腾的用户）

archinstall 会自动创建：

EFI 分区（约 1GB，挂载到 /boot）
根分区（剩余空间，挂载到 /）

如果你是双系统（保留 Windows），选 Manual Partitioning 手动指定，只格式化 Linux 分区，不要动 EFI 分区（选择已有 EFI 分区挂载即可）。

3.5 Bootloader（引导程序）推荐选 Grub。GRUB 支持多系统引导，界面友好，文档丰富，是最通用的选择。
如果是纯 Linux 单系统且使用 UEFI，也可选 systemd-boot，更轻量简洁。

3.6 Swap（交换空间）Swap 是当内存不足时用磁盘充当临时内存的机制。
推荐启用 zram（默认选项）。zram 是一种在内存中创建压缩 swap 的技术，比传统 swap 分区更快、更省磁盘空间，非常适合现代系统。

3.7 Hostname（主机名）给你的电脑起个名字，会显示在终端提示符里，比如 user@myhostname。
随意起，比如 archlinux、myarch、dev-machine 等。

3.8 Root password（root 密码）设置管理员账户 root 的密码。虽然日常使用建议用普通用户+sudo，但 root 密码仍需设置。

3.9 User account（用户账户）创建一个日常使用的普通用户，点击 Add a user：

输入用户名（小写字母，如 canary）
输入该用户的密码
是否加入 sudo 组：选 Yes，这样该用户可以用 sudo 执行管理员命令


3.10 Profile（预设配置）这是 archinstall 的核心选项，决定安装哪种预设环境。
选 Desktop，然后选择你想要的桌面环境：
选项 A：GNOME（推荐新手）GNOME 是功能最完整、开箱即用的桌面环境，适合从 Windows&#x2F;macOS 迁移过来的用户。界面简洁，触摸板手势流畅，应用生态完整。
选中 Gnome 即可，archinstall 会安装 GNOME 桌面及 GDM 显示管理器。
选项 B：KDE PlasmaKDE Plasma 是功能最丰富、高度可定制的桌面环境，界面接近 Windows，适合喜欢折腾外观的用户。
选中 KDE Plasma，archinstall 会安装 Plasma 桌面及 SDDM 显示管理器。
选项 C：Niri（平铺式 Wayland 合成器）Niri 是一个专为 Wayland 设计的滚动式平铺窗口管理器，窗口排列在无限延伸的水平工作区上，用滚动的方式导航。非常适合多开窗口的开发场景，但没有传统桌面环境那样完整的配套工具，需要配合 DMS 等工具使用（见第四篇）。
在 archinstall 的 Desktop 列表中选 Niri。
选项 D：Hyprland（动态平铺式 Wayland 合成器）Hyprland 是目前最流行的平铺式 Wayland 合成器，以流畅的动画效果和高度可定制性著称，在程序员和 Linux 爱好者中极为流行。同样需要配合额外工具使用。
在 archinstall 的 Desktop 列表中选 Hyprland。

建议：如果你是 Linux 新手，先选 GNOME 熟悉环境；熟悉后再尝试 Niri 或 Hyprland，体验平铺式工作流的魅力。


3.11 Audio（音频）推荐选 Pipewire。PipeWire 是现代 Linux 的音频系统，同时兼容原来的 PulseAudio 和 JACK 协议，支持低延迟音频，是目前的行业标准。

3.12 Kernels（内核）保持默认的 linux（标准内核）即可。
其他选项说明：

linux-lts：长期支持内核，更稳定，适合服务器
linux-zen：针对桌面使用优化，响应更灵敏，适合游戏


3.13 Network configuration（网络配置）选 NetworkManager。NetworkManager 是 GNOME、KDE 等桌面环境标配的网络管理工具，支持 Wi-Fi、有线、VPN 等，有图形化界面。

3.14 Timezone（时区）选择 Asia&#x2F;Shanghai（中国标准时间，CST，UTC+8）。
在列表中可以先按 / 搜索 “Shanghai” 快速定位。

3.15 开始安装所有选项配置完成后，选择 Install 开始安装。archinstall 会：

格式化并挂载分区
从镜像站下载并安装所有软件包
生成系统配置文件
安装引导程序

整个过程根据网速大约需要 5~30 分钟。安装完成后，脚本会询问是否进入 chroot 进行额外配置，一般选 No 即可。
然后执行：
reboot

拔出 U 盘，系统重启后进入你全新的 Arch Linux！

四、安装后的第一件事4.1 验证系统登录后打开终端，验证一些基本信息：
# 查看系统版本uname -a# 查看已安装的内核pacman -Q linux# 验证网络ping -c 3 archlinux.org

4.2 更新系统Arch 是滚动更新发行版，安装完立即同步一次软件包数据库：
sudo pacman -Syu


-S：同步（安装&#x2F;更新）
-y：刷新软件包数据库
-u：升级所有已安装的包

这个命令在日后也是定期维护系统的核心命令，建议每周执行一次。
4.3 检查镜像源速度如果更新速度慢，安装 reflector 并重新选择最快的国内源：
sudo pacman -S reflectorsudo reflector --country China --age 12 --protocol https --sort rate --save /etc/pacman.d/mirrorlistsudo pacman -Syu


小结通过 archinstall，我们完成了：

制作启动 U 盘，从 Live 环境启动
配置镜像、分区、用户、桌面环境等所有安装选项
安装 GNOME &#x2F; KDE Plasma &#x2F; Niri &#x2F; Hyprland 桌面
安装完成后的基本验证和系统更新

下一篇：通过 WSL 在 Windows 上安装 Arch Linux，适合不想折腾物理机、只想在 Windows 里体验 Arch 的读者。
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>Arch Linux</tag>
        <tag>archinstall</tag>
        <tag>安装教程</tag>
      </tags>
  </entry>
  <entry>
    <title>通过 WSL 安装 Arch Linux</title>
    <url>/2026/06/17/linux/02-WSL%E5%AE%89%E8%A3%85Arch-Linux/</url>
    <content><![CDATA[通过 WSL 安装 Arch Linux
适用读者：Windows 用户，想在不折腾物理机、不装双系统的情况下，在 Windows 里使用完整的 Arch Linux 开发环境。


前言：WSL 是什么？WSL（Windows Subsystem for Linux）是微软内置于 Windows 10&#x2F;11 的 Linux 兼容层，让你可以在 Windows 里运行一个真实的 Linux 内核和发行版。WSL2 使用完整的 Linux 内核（而不是模拟层），性能接近原生，文件系统、网络、进程隔离都更完善。
WSL 的优点：

无需重启，Windows 和 Linux 同时运行
可以直接访问 Windows 文件（/mnt/c/ 等）
支持 GUI 应用（WSLg）
开发工具链（git、Python、Node.js、Docker 等）可以原生运行在 Linux 环境

WSL 默认只提供 Ubuntu，但 Arch Linux 可以通过第三方方式安装进来，享受 Arch 的滚动更新和 AUR 生态。

一、启用 WSL21.1 系统要求
Windows 10 版本 2004（内部版本 19041）或更高，推荐 Windows 11
已开启虚拟化（BIOS 里的 Intel VT-x 或 AMD-V）

1.2 安装 WSL以管理员身份打开 PowerShell（右键开始菜单 → Windows Terminal（管理员）），执行：
wsl --install

这条命令会自动：

启用”适用于 Linux 的 Windows 子系统”功能
启用”虚拟机平台”功能
下载并安装最新版 Linux 内核
将 WSL2 设置为默认版本
安装 Ubuntu（我们稍后不用它）

执行完成后，重启电脑。
1.3 验证 WSL2 版本重启后，打开 PowerShell 确认 WSL 版本：
wsl --version

输出类似：
WSL 版本：2.x.x.x内核版本：5.15.x.x

确认是 WSL2 即可。

二、安装 Arch Linux官方 WSL 应用商店没有 Arch Linux，但有两种主流方式安装：
方式 A：通过 Microsoft Store（推荐）微软应用商店里有一个非官方但广受认可的 Arch Linux 应用（由 yuk7 维护）：

打开 Microsoft Store
搜索 Arch Linux
找到发布者为 “Arch Linux” 的应用并安装
安装完成后，在开始菜单搜索 “Arch Linux” 打开即可

方式 B：手动导入（ArchWSL）如果 Store 无法访问，可以手动安装 ArchWSL：

前往 GitHub Releases 下载最新的 Arch.zip：
https://github.com/yuk7/ArchWSL/releases/latest

将 Arch.zip 解压到你想长期存放这个 Linux 系统的目录，比如：
C:\WSL\Arch\

⚠️ 不要放在 C:\Program Files 等系统目录，避免权限问题。


双击目录里的 Arch.exe，等待安装完成（约 1 分钟）。

安装完成后，打开 PowerShell 确认：
wsl -l -v
能看到 Arch 且版本为 2 即成功。



三、初始化 Arch Linux第一次打开 Arch Linux 终端时，你是 root 用户。需要做一些初始化。
3.1 初始化 pacman 密钥环密钥环（keyring）用于验证软件包的签名，确保安全性。全新安装的 Arch 需要手动初始化：
pacman-key --initpacman-key --populate archlinux

这两条命令会生成本机的密钥对，并导入 Arch Linux 官方的信任密钥库。如果跳过这步，安装软件包时会报签名验证错误。
3.2 更新软件包数据库pacman -Syu

第一次运行会下载并安装大量更新，耐心等待即可。

四、配置国内镜像源默认的 Arch 镜像是全球随机选取的，在国内速度可能很慢。我们手动配置国内源。
4.1 编辑镜像列表nano /etc/pacman.d/mirrorlist

将文件内容全部替换为以下国内镜像（保留几个备用）：
# 清华大学Server = https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$arch# 中科大Server = https://mirrors.ustc.edu.cn/archlinux/$repo/os/$arch# 阿里云Server = https://mirrors.aliyun.com/archlinux/$repo/os/$arch# 浙江大学Server = https://mirrors.zju.edu.cn/archlinux/$repo/os/$arch

保存退出：Ctrl+O → Enter → Ctrl+X
4.2 为什么要配置镜像源？pacman 下载软件包时会按照 /etc/pacman.d/mirrorlist 文件中的顺序逐个尝试。文件越靠前的服务器优先级越高。国内镜像服务器与 Arch 官方同步延迟一般不超过数小时，完全够用，而速度比直连海外快 5~10 倍。
4.3 刷新并更新pacman -Syu

再次执行更新，这次应该速度明显加快。

五、创建普通用户在 Linux 里长期以 root 身份操作是危险的——一条错误的命令就可能损坏系统。创建一个普通用户用于日常使用。
5.1 安装 sudopacman -S sudo

sudo 让普通用户在需要时以管理员权限执行特定命令，而不需要切换到 root。
5.2 创建用户# useradd -m 表示自动创建家目录，-G wheel 表示加入 wheel 组useradd -m -G wheel 你的用户名# 设置密码passwd 你的用户名

5.3 配置 sudo 权限编辑 sudo 配置文件：
EDITOR=nano visudo

找到这一行（被 # 注释掉了）：
# %wheel ALL=(ALL:ALL) ALL

去掉前面的 #，变成：
%wheel ALL=(ALL:ALL) ALL

这条配置的意思是：wheel 用户组里的所有成员（%wheel）在任何主机（第一个 ALL）上，以任何用户身份（(ALL:ALL)），都可以执行任何命令（最后一个 ALL）。
保存退出。
5.4 设置默认用户在 PowerShell 里，将 Arch WSL 的默认登录用户改为你创建的普通用户：
# 将 &quot;你的用户名&quot; 替换为实际用户名Arch config --default-user 你的用户名

之后每次打开 Arch Linux 终端，都会直接以普通用户身份登录。

六、基本配置优化6.1 配置语言（中文支持）编辑 locale 配置：
sudo nano /etc/locale.gen

找到并取消注释以下两行（去掉行首的 #）：
en_US.UTF-8 UTF-8zh_CN.UTF-8 UTF-8

生成 locale：
sudo locale-gen

设置系统默认语言。WSL 环境下建议保持英文（避免终端中文乱码），仅启用中文支持：
sudo nano /etc/locale.conf

写入：
LANG=en_US.UTF-8

6.2 配置时区sudo ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime

ln -sf 创建一个软链接，将时区文件链接到 /etc/localtime。Arch 通过这个文件判断当前时区。WSL 默认会同步 Windows 的时间，所以时间本身不会错，但时区配置影响一些程序显示时间的方式。
6.3 配置主机名sudo nano /etc/hostname

写入一个主机名，比如：
archlinux-wsl


七、WSL 常用技巧7.1 访问 Windows 文件Windows 的各个盘符挂载在 /mnt/ 下：
ls /mnt/c/Users/        # 访问 C 盘用户目录ls /mnt/d/              # 访问 D 盘

7.2 在 Windows 中打开 Linux 文件在任意路径执行：
explorer.exe .

会直接在 Windows 资源管理器里打开当前目录。
7.3 设置 WSL 资源限制WSL2 默认会占用最多 50% 的物理内存。可以在 Windows 用户目录下创建 .wslconfig 文件进行限制：
在 PowerShell 里：
notepad $env:USERPROFILE\.wslconfig

写入：
[wsl2]memory=8GB        # 最多使用 8GB 内存processors=4      # 最多使用 4 个 CPU 核心swap=2GB          # 交换空间大小

修改后需要重启 WSL：
wsl --shutdown

再重新打开 Arch Linux。
7.4 WSL 常用命令（在 PowerShell 中执行）wsl -l -v           # 列出所有 WSL 发行版及状态wsl --shutdown      # 关闭所有运行中的 WSL 实例wsl -d Arch         # 启动名为 Arch 的发行版wsl --export Arch D:\backup\arch-backup.tar  # 导出备份wsl --import Arch D:\WSL\Arch D:\backup\arch-backup.tar  # 从备份恢复


小结通过 WSL2，我们在 Windows 上安装了完整的 Arch Linux，并完成了：

启用 WSL2 并安装 Arch Linux
初始化 pacman 密钥环，配置国内镜像源
创建普通用户并配置 sudo 权限
设置语言、时区、主机名等基本配置
了解 WSL 与 Windows 互操作的常用技巧

下一篇：安装通用开发工具，在 Arch Linux（无论是实体机还是 WSL）上搭建完整的开发环境。
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>Arch Linux</tag>
        <tag>WSL</tag>
        <tag>Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>安装通用开发工具</title>
    <url>/2026/06/17/linux/03-%E5%AE%89%E8%A3%85%E9%80%9A%E7%94%A8%E5%BC%80%E5%8F%91%E5%B7%A5%E5%85%B7/</url>
    <content><![CDATA[安装通用开发工具
适用读者：已完成 Arch Linux 安装（无论实体机还是 WSL），想搭建完整开发环境的读者。本文所有步骤在两种环境下均适用。


前言：我们要装什么？一个典型的全栈开发环境需要：



工具
用途



git
版本控制，几乎所有项目必备


yay
AUR 助手，扩展 Arch 软件源


sdkman
Java&#x2F;Maven 版本管理


nvm
Node.js 版本管理


uv
Python 虚拟环境和包管理（Rust 写的，极快）


go
Go 语言工具链


docker
容器化运行环境


neovim + LazyVim
高效终端编辑器


kitty
GPU 加速终端模拟器（实体机）


zip &#x2F; unzip
压缩解压工具（sdkman 依赖）



一、安装 git（通过 pacman）为什么先用 pacman 安装 git？
后续安装 yay（AUR 助手）需要从 GitHub 克隆代码，而克隆就需要 git。pacman 是 Arch 内置的包管理器，git 在官方源里有，直接装：
sudo pacman -S git

验证安装：
git --version# 输出类似：git version 2.x.x

配置你的身份（git commit 时会用到）：
git config --global user.name &quot;你的名字&quot;git config --global user.email &quot;你的邮箱&quot;


二、安装 yay（AUR 助手）2.1 什么是 AUR？AUR（Arch User Repository）是 Arch Linux 的社区软件仓库，里面有官方源没有的软件，比如各种专有软件、开发工具、字体等。官方源用 pacman 管理，AUR 需要 AUR 助手来自动化编译安装过程。
yay 是目前最流行的 AUR 助手，它完全兼容 pacman 的命令语法，你可以把它当 pacman 的超集来用——yay 既能装官方源的包，也能装 AUR 的包。
2.2 安装前置依赖yay 本身需要 base-devel（编译工具集）才能从 AUR 编译软件：
sudo pacman -S base-devel

base-devel 包含 gcc、make、binutils 等编译必需的工具链。
2.3 克隆并编译 yay# 克隆 yay 的 AUR 构建脚本到临时目录git clone https://aur.archlinux.org/yay.git /tmp/yay# 进入目录cd /tmp/yay# 编译并安装（-si 表示安装依赖并安装软件包）makepkg -si


⚠️ AUR 的软件不能以 root 身份编译，请确保你是普通用户（提示符末尾是 $ 而不是 #）。

安装完成后验证：
yay --version


三、使用 yay 批量安装开发工具有了 yay，后续安装无论官方包还是 AUR 包，语法完全一致。
3.1 安装 zip 和 unzip（sdkman 的前置依赖）sdkman 的安装脚本和下载逻辑依赖 zip 和 unzip，必须提前装好：
yay -S zip unzip

3.2 批量安装其余工具yay -S git yay sdkman nvm uv kitty go docker neovim


说明：

git 和 yay 已经装了，再执行不会报错，yay 会提示”已是最新版本”并跳过
kitty 是 GPU 加速终端，WSL 环境下用不上（WSL 没有独立的窗口系统），实体机用户安装
sdkman 和 nvm 在 AUR 里，yay 会自动处理编译



四、安装并配置 LazyVim4.1 什么是 LazyVim？LazyVim 是基于 Neovim 的一套完整配置框架，集成了 LSP（语言服务器协议，实现代码补全、跳转定义等）、文件树、模糊搜索、git 集成等常用插件，开箱即用，同时保持高度可扩展性。
LazyVim 是本次配置中唯一不由 yay 管理的组件，它通过官方推荐的方式直接以配置文件形式安装，由 Neovim 的插件管理器 lazy.nvim 在首次启动时自动下载。
4.2 备份旧配置（如有）# 备份已有的 Neovim 配置（如果有的话）mv ~/.config/nvim ~/.config/nvim.bak 2&gt;/dev/nullmv ~/.local/share/nvim ~/.local/share/nvim.bak 2&gt;/dev/nullmv ~/.local/state/nvim ~/.local/state/nvim.bak 2&gt;/dev/nullmv ~/.cache/nvim ~/.cache/nvim.bak 2&gt;/dev/null

4.3 克隆 LazyVim 起始配置LazyVim 官方提供了一个 starter 仓库，直接克隆到 Neovim 配置目录：
git clone https://github.com/LazyVim/starter ~/.config/nvim# 删除 .git 目录，方便后续用自己的 git 管理配置rm -rf ~/.config/nvim/.git

4.4 首次启动nvim

第一次打开 Neovim，lazy.nvim 会自动下载并安装所有插件（需要访问 GitHub，国内可能较慢，耐心等待）。
安装完成后界面会变得非常漂亮，有文件树、状态栏、语法高亮等。
常用快捷键：

Space：打开命令菜单（LazyVim 的核心入口）
Space + e：打开&#x2F;关闭文件树
Space + ff：模糊搜索文件
:q 或 :wq：退出


五、配置 sdkman（Java&#x2F;Maven 版本管理）5.1 什么是 sdkman？sdkman 是 JVM 生态的版本管理工具，可以方便地安装和切换不同版本的 JDK、Maven、Gradle 等工具。类似 Python 的 pyenv、Node.js 的 nvm。
5.2 激活 sdkman通过 yay 安装的 sdkman 包会在 /opt/sdkman 下放置文件，但还需要在 shell 配置文件中激活它：
# 激活 sdkman（这条命令也需要加入 .bashrc，见第九节）source /opt/sdkman/bin/sdkman-init.sh

验证：
sdk version

5.3 基本命令# 列出可安装的 JDK 版本sdk list java# 安装 JDK 21（LTS 版本，推荐）sdk install java 21.0.x-tem# 安装特定的 JDK，比如 Temurin（原 AdoptOpenJDK）sdk install java 21.0.5-tem# 安装 Mavensdk install maven# 查看当前使用的版本sdk current javasdk current maven# 在当前 shell 临时切换版本sdk use java 17.0.x-tem# 设置默认版本sdk default java 21.0.x-tem

5.4 安装 JDK 和 Maven# 安装 JDK 21（当前 LTS 版本）sdk install java# 安装 Mavensdk install maven

直接 sdk install java 不指定版本时，会安装 sdkman 推荐的默认版本。
验证：
java -versionmvn -version


六、配置 nvm（Node.js 版本管理）6.1 什么是 nvm？nvm（Node Version Manager）允许你在同一台机器上安装和切换多个 Node.js 版本。不同项目可能需要不同的 Node.js 版本，nvm 让切换变得简单。
6.2 激活 nvm通过 yay 安装的 nvm 包同样需要在 shell 配置中激活（见第九节，这里先手动激活验证）：
source /usr/share/nvm/init-nvm.sh

6.3 基本命令# 列出可用的 Node.js 版本nvm ls-remote# 安装最新 LTS 版本nvm install --lts# 安装指定版本nvm install 20# 列出已安装的版本nvm ls# 切换版本nvm use 20# 设置默认版本（新 shell 会使用此版本）nvm alias default 20

6.4 安装 Node.js# 安装当前 LTS 版本nvm install --lts# 设为默认nvm alias default node

验证：
node --versionnpm --version


七、配置 uv（Python 环境管理）7.1 什么是 uv？uv 是由 Astral 公司用 Rust 编写的 Python 包管理和项目管理工具，速度比 pip 快 10~100 倍。它整合了虚拟环境创建、包安装、Python 版本管理等功能。
为什么不用 pyenv？
Arch Linux 默认会预装系统 Python（python3），日常开发直接用 uv 创建虚拟环境即可，不需要用 pyenv 再管理一套 Python 版本。uv 自身也可以安装指定版本的 Python。
7.2 基本命令# 查看 uv 版本uv --version# 创建虚拟环境（在当前目录创建 .venv/）uv venv# 激活虚拟环境source .venv/bin/activate# 在虚拟环境中安装包uv pip install requests fastapi# 退出虚拟环境deactivate# 使用项目模式（推荐，会自动管理虚拟环境）uv init myproject    # 创建新项目cd myprojectuv add requests      # 添加依赖（自动创建虚拟环境）uv run python main.py  # 在虚拟环境中运行脚本# 安装特定版本的 Pythonuv python install 3.12


八、配置 Go8.1 设置 GOPATH 和 GOMODCACHEGo 安装完后，默认的工作目录是 ~/go，通常不需要修改。但在国内，访问 Go 官方模块代理（proxy.golang.org）会很慢，需要配置国内代理：
# 设置国内模块代理（七牛云）go env -w GOPROXY=https://goproxy.cn,direct# 设置模块私有化白名单（按需，公司内网仓库用）# go env -w GONOSUMCHECK=your.private.domain

GOPROXY=https://goproxy.cn,direct 的含义：先从 goproxy.cn 下载，如果没有再直连（direct），兼容私有模块。
8.2 基本命令# 查看 Go 版本go version# 初始化一个新模块go mod init myapp# 下载依赖go mod tidy# 编译go build -o myapp .# 运行go run main.go# 安装工具（安装到 $GOPATH/bin/，需要将其加入 PATH）go install golang.org/x/tools/gopls@latest

8.3 将 Go 工具目录加入 PATHGo 安装的工具（通过 go install）默认放在 ~/go/bin/，需要将这个目录加入 PATH 才能直接运行。这在第九节的 .bashrc 配置中一并完成。

九、.bashrc 配置详解.bashrc 是 bash shell 每次启动时自动执行的配置文件，里面放置环境变量、PATH 配置、alias 等。
编辑：
nano ~/.bashrc

完整配置如下（含解析）：
# ===== 系统自带部分（通常已存在，不需要动） =====# 如果不是交互式 shell，直接退出（脚本中被 source 时的保护）[ -z &quot;$PS1&quot; ] &amp;&amp; return# 历史记录设置HISTCONTROL=ignoredups:ignorespace   # 忽略重复命令和以空格开头的命令HISTSIZE=1000                        # 内存中保留 1000 条历史HISTFILESIZE=2000                    # 历史文件最多 2000 条shopt -s histappend    # 多个终端同时运行时，历史追加而非覆盖shopt -s checkwinsize  # 每条命令后自动更新终端窗口大小变量# ===== 终端提示符（PS1）=====# 自定义一个简洁、带颜色的提示符# 格式：用户名@主机名 当前目录 $# 颜色说明：\033[36m = 青色，\033[33m = 黄色，\033[32m = 绿色，\033[0m = 重置颜色PS1=&#x27;\[\033[36m\]\u@\h\[\033[0m\] \[\033[33m\]\W\[\033[0m\] \[\033[32m\]$ \[\033[0m\]&#x27;# ===== PATH 扩展 =====# Go 工具安装目录export PATH=&quot;$HOME/go/bin:$PATH&quot;# ===== sdkman 初始化 =====# sdkman 通过修改 PATH 来切换 JDK/Maven 版本# THIS MUST BE AT THE END OF THE FILE for sdkman to work!!!export SDKMAN_DIR=&quot;/opt/sdkman&quot;[[ -s &quot;$SDKMAN_DIR/bin/sdkman-init.sh&quot; ]] &amp;&amp; source &quot;$SDKMAN_DIR/bin/sdkman-init.sh&quot;# ===== nvm 初始化 =====# nvm 同样通过修改 PATH 来切换 Node.js 版本source /usr/share/nvm/init-nvm.sh

关键点解析：

\[\033[36m\]：ANSI 转义序列，设置终端颜色。\[...\] 包裹是告诉 bash 这部分不可见，用于正确计算光标位置。36m 是青色。
\u：当前用户名；\h：主机名；\W：当前目录名（仅最后一级）
\[\033[0m\]：重置颜色，防止颜色”溢出”影响后续输出
sdkman 必须在文件末尾：sdkman 的注释里明确要求这一点，因为它需要最后修改 PATH 以确保优先级

修改完 .bashrc 后，让当前 shell 加载新配置：
source ~/.bashrc


十、Docker 配置10.1 启动并设置 Docker 开机自启Docker 安装后不会自动启动，需要手动开启：
# 启动 Docker 服务sudo systemctl start docker# 设置开机自动启动 Dockersudo systemctl enable docker

为什么要用 systemctl？
Arch Linux 使用 systemd 作为 init 系统，所有后台服务（守护进程）通过 systemctl 管理。enable 命令会在 /etc/systemd/system/ 下创建符号链接，让系统启动时自动拉起 Docker 守护进程。
10.2 将用户加入 docker 组默认情况下，运行 docker 命令需要 sudo。将当前用户加入 docker 组可以避免每次都输密码：
sudo usermod -aG docker $USER

重新登录后生效（或执行 newgrp docker 立即在当前 shell 生效）。
10.3 基本命令# 验证 Docker 安装docker --versiondocker run hello-world# 镜像操作docker pull nginx                 # 拉取镜像docker images                     # 列出本地镜像docker rmi nginx                  # 删除镜像# 容器操作docker run -d -p 8080:80 nginx   # 后台运行 nginx，映射端口 8080→80docker ps                         # 列出运行中的容器docker ps -a                      # 列出所有容器（含已停止）docker stop &lt;容器ID&gt;              # 停止容器docker rm &lt;容器ID&gt;                # 删除容器docker logs &lt;容器ID&gt;              # 查看容器日志# 进入运行中的容器docker exec -it &lt;容器ID&gt; bash# Docker Composedocker compose up -d              # 后台启动docker compose down               # 停止并删除docker compose logs -f            # 跟踪日志

10.4 WSL 中的 Docker 注意事项WSL2 环境下，systemd 默认不启用。可以在 /etc/wsl.conf 中启用它：
sudo nano /etc/wsl.conf

写入：
[boot]systemd=true

重启 WSL（在 PowerShell 执行 wsl --shutdown）后，systemctl 命令就能正常使用了。
或者，如果不想开启 systemd，也可以手动启动 Docker：
sudo dockerd &amp;


小结这一篇完成了完整开发环境的搭建：

git → 版本控制基础
yay → 打开 AUR 生态的大门
sdkman → Java&#x2F;Maven 多版本管理
nvm → Node.js 多版本管理
uv → 现代化 Python 环境管理
Go → Go 工具链及国内代理配置
LazyVim → 开箱即用的 Neovim 配置
.bashrc → 将所有配置串联起来
Docker → 容器化运行环境

下一篇：niri 和 Hyprland 配置，通过 DMS 让你的窗口管理器真正”能用”起来。
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>Python</tag>
        <tag>Java</tag>
        <tag>开发环境</tag>
        <tag>yay</tag>
        <tag>Neovim</tag>
        <tag>Docker</tag>
        <tag>Node.js</tag>
        <tag>Go</tag>
      </tags>
  </entry>
  <entry>
    <title>niri 和 Hyprland 配置（DMS）</title>
    <url>/2026/06/17/linux/04-niri%E5%92%8CHyprland%E9%85%8D%E7%BD%AEDMS/</url>
    <content><![CDATA[niri 和 Hyprland 配置（DMS）
适用读者：已在实体机上安装 niri 或 Hyprland 的读者。WSL 用户跳过此篇（WSL 没有独立的 Wayland 显示服务器）。


前言：窗口管理器 ≠ 桌面环境安装完 niri 或 Hyprland 后，你会发现它们只是”窗口管理器”——负责排列、移动窗口。一个完整可用的桌面还需要：

状态栏（显示时间、网络、音量、电量）
应用启动器（搜索和打开应用）
通知系统（消息弹窗）
锁屏程序
壁纸管理
屏幕截图工具
剪贴板管理

把这些一个个手动安装配置，需要几天时间。DMS（Dank Material Shell） 是一个一站式的桌面 Shell，设计目标就是让 niri 和 Hyprland 开箱即用，界面遵循 Material Design 3 风格，外观现代、功能完整。

一、DMS 简介DMS 全称 DankMaterialShell，是基于 Quickshell（一个 QML 驱动的 Shell 框架）和 Go 构建的桌面 Shell。它能替代以下独立工具：



替代的工具
DMS 对应功能



Waybar
TopBar 状态栏


Swaylock &#x2F; Hyprlock
锁屏


Mako &#x2F; Dunst
通知系统


Fuzzel &#x2F; Rofi
应用启动器（Spotlight）


Swww &#x2F; Swaybg
壁纸管理


cliphist
剪贴板历史


wl-clipboard
Wayland 剪贴板工具


此外，DMS 还提供：

基于壁纸自动生成配色方案（matugen 驱动）
CPU &#x2F; RAM &#x2F; GPU 监控（dgop 驱动）
工作区切换、媒体播放控制、系统托盘等


二、前置条件确认在安装 DMS 之前，请确认以下软件已经安装：
# 确认 niri 或 hyprland 已安装which niriwhich hyprland# 确认 kitty 已安装（DMS 默认终端）which kitty# 确认 neovim 已安装which nvim

如果缺少，回到第三篇用 yay 安装。

三、安装 DMS方式 A：通过官方安装脚本（推荐）DMS 提供了一个交互式安装脚本，会自动处理所有依赖：
curl -fsSL https://install.danklinux.com | sh

脚本会：

检测你的发行版（Arch Linux）
从官方源安装 dms-shell
从 AUR 编译安装 quickshell、matugen、dgop
引导你选择合成器（niri 或 Hyprland）

方式 B：手动安装如果不信任脚本或网络受限，可以手动安装：
# 安装 dms-shell（Arch 官方 extra 源，无需编译）sudo pacman -S dms-shell# 安装 niri 或 hyprland 的 DMS 集成包（二选一）sudo pacman -S dms-shell-niri       # niri 用户sudo pacman -S dms-shell-hyprland  # Hyprland 用户# 安装 Quickshell（DMS 的核心 QML 框架，AUR）yay -S quickshell# 安装 matugen（壁纸自动配色，可选但推荐）yay -S matugen# 安装 dgop（系统资源监控，可选）yay -S dgop

安装辅助工具# 剪贴板管理sudo pacman -S cliphist wl-clipboard# 截图工具sudo pacman -S grim slurp  # niri 用户# hyprland 用户可用 hyprshot：yay -S hyprshot


四、运行 dms setupDMS 安装完成后，运行配置向导。这个命令会自动修改你的合成器配置文件，加入 DMS 的快捷键和自启动项：
dms setup

setup 向导会询问你：

选择合成器：niri 或 Hyprland（根据你安装的选）
选择终端：Kitty（推荐）、Ghostty 或 Alacritty
是否备份现有配置：推荐选 Yes，setup 会在修改前备份原文件

setup 完成后，它实际做了什么？

向 ~/.config/niri/config.kdl（或 ~/.config/hypr/hyprland.conf）追加了 DMS 自启动配置和快捷键绑定
创建了 ~/.config/dms/ 目录，放入 DMS 的用户配置文件
设置了 DMS 作为状态栏、通知、锁屏的默认程序


五、niri 配置详解niri 的配置文件格式是 KDL（Kubernetes Document Language 的变体，和 JSON&#x2F;YAML 并列的配置语言），位于 ~/.config/niri/config.kdl。
dms setup 之后，你的 config.kdl 会包含以下关键内容：
// 开机自启动 DMS Shellspawn-at-startup &quot;dms&quot; &quot;run&quot;// 快捷键（Super = Win 键）binds &#123;    Super+Return &#123; spawn &quot;kitty&quot;; &#125;    Super+Space  &#123; spawn &quot;dms&quot; &quot;ipc&quot; &quot;call&quot; &quot;spotlight&quot; &quot;toggle&quot;; &#125;  // 应用启动器    Super+L      &#123; spawn &quot;dms&quot; &quot;ipc&quot; &quot;call&quot; &quot;lock&quot;; &#125;                // 锁屏    Super+Q      &#123; close-window; &#125;    // ... 更多快捷键由 dms setup 生成&#125;// 输出配置（显示器）output &quot;eDP-1&quot; &#123;    scale 1.5    // HiDPI 缩放，根据你的屏幕调整&#125;

niri 的核心概念：滚动式工作区
niri 没有传统的”多桌面”概念，窗口排列在一条无限延伸的水平轴上。用鼠标滚轮或触摸板横向滑动来浏览。每个”列”可以有多个垂直排列的窗口。
常用快捷键：

Super + Enter：打开终端（kitty）
Super + Space：打开 DMS 应用启动器
Super + ←/→：切换焦点到左&#x2F;右列
Super + Shift + ←/→：将当前窗口移动到左&#x2F;右
Super + Q：关闭当前窗口
Super + F：切换全屏


六、Hyprland 配置详解6.1 配置文件格式Hyprland 0.55 之后默认使用 Lua 配置（~/.config/hypr/hyprland.lua），旧的 hyprlang 格式（hyprland.conf）仍然支持，但官方建议迁移到 Lua。

如果你安装的是 Hyprland 0.54 或更早版本，配置文件是 hyprland.conf，语法不同。

dms setup 会根据你的 Hyprland 版本选择合适的格式写入配置。
6.2 dms setup 写入的核心配置以 Lua 格式（0.55+）为例，setup 后的关键内容：
-- 环境变量：让所有应用使用 Wayland 原生渲染hl.env(&quot;GDK_BACKEND&quot;, &quot;wayland,x11&quot;)hl.env(&quot;QT_QPA_PLATFORM&quot;, &quot;wayland;xcb&quot;)hl.env(&quot;CLUTTER_BACKEND&quot;, &quot;wayland&quot;)hl.env(&quot;SDL_VIDEODRIVER&quot;, &quot;wayland&quot;)-- 开机自启动 DMShl.on(&quot;hyprland.start&quot;, function()    hl.exec_cmd(&quot;dms run&quot;)    hl.exec_cmd(&quot;dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP&quot;)end)-- 快捷键hl.bind(&quot;SUPER RETURN&quot;, function() hl.exec_cmd(&quot;kitty&quot;) end)hl.bind(&quot;SUPER SPACE&quot;, function() hl.exec_cmd(&quot;dms ipc call spotlight toggle&quot;) end)hl.bind(&quot;SUPER L&quot;, function() hl.exec_cmd(&quot;dms ipc call lock&quot;) end)hl.bind(&quot;SUPER Q&quot;, function() hl.dispatch(&quot;killactive&quot;) end)

旧格式（0.54 及以下，hyprlang）：
# 环境变量env = GDK_BACKEND,wayland,x11env = QT_QPA_PLATFORM,wayland;xcb# 开机自启exec-once = dms runexec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP# 快捷键bind = SUPER, Return, exec, kittybind = SUPER, Space, exec, dms ipc call spotlight togglebind = SUPER, L, exec, dms ipc call lockbind = SUPER, Q, killactive

6.3 Hyprland 的动态平铺Hyprland 使用传统的多工作区模型（类似 i3），但支持丰富的动画效果。窗口可以在工作区之间移动，支持浮动模式。
常用快捷键（DMS 配置后）：

Super + Enter：打开终端
Super + Space：DMS 应用启动器
Super + 1~9：切换工作区
Super + Shift + 1~9：将窗口移动到指定工作区
Super + Q：关闭窗口
Super + F：全屏
Super + V：浮动&#x2F;平铺切换


七、启动桌面环境niri从 TTY 登录后执行：
niri-session

或者如果安装了显示管理器（如 GDM），在登录界面选择 “niri” 会话。
Hyprland从 TTY 执行：
Hyprland

或通过显示管理器选择 “Hyprland” 会话。

八、DMS 常用命令DMS 安装后提供了 dms CLI 工具：
# 启动 DMS Shelldms run# 通过 IPC 控制 DMSdms ipc call spotlight toggle      # 打开/关闭应用启动器dms ipc call lock                   # 立即锁屏dms ipc call wallpaper set /path/to/image.jpg  # 设置壁纸dms ipc call audio setvolume 50    # 设置音量到 50%# 截图dms screenshot                      # 截图并复制到剪贴板dms screenshot --region            # 区域截图# 亮度控制（需要 i2c-tools 或笔记本内置显示器）dms brightness list                # 列出显示器dms brightness set 80              # 设置亮度到 80%# 剪贴板历史dms clipboard                      # 打开剪贴板历史选择器# 诊断dms doctor                         # 检查依赖和配置是否正常


九、调整和个性化9.1 换壁纸并自动配色DMS 的亮点功能之一：设置壁纸后，matugen 会自动提取颜色并重新主题化整个桌面（状态栏、终端、GTK 应用等）：
dms ipc call wallpaper set ~/Pictures/my-wallpaper.jpg

9.2 查看所有可用快捷键dms keybinds

9.3 更新 DMSsudo pacman -Syu dms-shell    # 更新 dms-shell 本体yay -Syu quickshell           # 更新 quickshell


小结这一篇完成了：

了解 DMS 的定位——一站式替代零散的桌面工具
通过安装脚本或手动安装 DMS 和依赖
运行 dms setup 自动配置合成器
了解 niri 和 Hyprland 各自的配置格式和核心概念
掌握 DMS 的日常使用命令

下一篇：中文环境配置和中文输入法，让你的 Arch Linux 完整支持中文显示和输入。
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>niri</tag>
        <tag>Hyprland</tag>
        <tag>DMS</tag>
        <tag>Wayland</tag>
        <tag>窗口管理器</tag>
      </tags>
  </entry>
  <entry>
    <title>中文环境配置和中文输入法</title>
    <url>/2026/06/17/linux/05-%E4%B8%AD%E6%96%87%E7%8E%AF%E5%A2%83%E5%92%8C%E8%BE%93%E5%85%A5%E6%B3%95%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[中文环境配置和中文输入法
适用读者：已完成前四篇配置、需要在 Arch Linux 上使用中文界面和输入法的读者。


前言：中文支持的两个层面在 Linux 上”用中文”包含两件事：

中文显示：系统界面、终端、应用程序能正确显示中文字符（需要正确的 locale 和字体）
中文输入：能通过拼音等方式输入汉字（需要输入法框架）

两者都需要配置，缺一不可。

一、配置系统中文环境（locale）1.1 什么是 locale？locale 是操作系统的本地化设置，决定系统使用的语言、字符编码、日期格式、货币符号等。zh_CN.UTF-8 就是”中国大陆简体中文、UTF-8 编码”的 locale。
1.2 生成中文 locale编辑 /etc/locale.gen，取消注释中文和英文的 UTF-8 行：
sudo nano /etc/locale.gen

找到并去掉这两行的 #：
en_US.UTF-8 UTF-8zh_CN.UTF-8 UTF-8

然后生成：
sudo locale-gen

为什么要同时保留英文 locale？
某些程序在中文 locale 下会产生乱码（尤其是终端里的错误信息、man 页面），保留 en_US.UTF-8 作为备选可以避免这类问题。
1.3 设置默认语言编辑 /etc/locale.conf：
sudo nano /etc/locale.conf

桌面环境用户（GNOME &#x2F; KDE &#x2F; niri &#x2F; Hyprland），设置为中文：
LANG=zh_CN.UTF-8

纯终端用户（WSL 或无桌面），建议保持英文，否则终端里的中文提示符和错误信息可能显示异常：
LANG=en_US.UTF-8

修改 /etc/locale.conf 后需要重新登录才能生效，或者执行：
export LANG=zh_CN.UTF-8

（仅对当前 shell 临时生效）
1.4 安装中文字体设置了中文 locale 但没有中文字体，屏幕上的汉字会显示为方块。安装常用字体：
# 思源黑体（Adobe/Google 联合发布，高质量开源字体）sudo pacman -S adobe-source-han-sans-cn-fonts# 思源宋体（衬线体）sudo pacman -S adobe-source-han-serif-cn-fonts# Noto 字体（Google 出品，覆盖所有 Unicode 字符，防止&quot;豆腐块&quot;）sudo pacman -S noto-fonts noto-fonts-cjk noto-fonts-emoji

安装后刷新字体缓存：
fc-cache -fv


二、GNOME 下配置中文输入法（ibus-pinyin）GNOME 内置了对 ibus 输入法框架的深度集成——GNOME 在登录时会自动启动 ibus 守护进程，无需额外配置启动。
2.1 什么是 ibus？ibus（Intelligent Input Bus）是 Linux 下的输入法框架，负责在应用程序和具体输入法引擎之间传递按键事件。GNOME 选择 ibus 作为官方支持的输入法框架。
2.2 安装 ibus-pinyinsudo pacman -S ibus ibus-pinyin


ibus：输入法框架本体
ibus-pinyin：拼音输入法引擎

2.3 配置 ibus打开 GNOME 的”设置”→”键盘”→”输入源”，点击 + 号，选择”汉语（中国）”→”中文（Intelligent Pinyin）”添加。
或者通过命令行配置：
# 添加中文拼音输入源gsettings set org.gnome.desktop.input-sources sources &quot;[(&#x27;xkb&#x27;, &#x27;us&#x27;), (&#x27;ibus&#x27;, &#x27;pinyin&#x27;)]&quot;

2.4 切换输入法GNOME 默认使用 Super + Space 切换输入法。也可以在设置里改成 Ctrl + Space 或其他快捷键。
2.5 为什么 GNOME 用 ibus 而不是 fcitx5？GNOME 通过 XDG Autostart 和 D-Bus 与 ibus 深度绑定，ibus 在 GNOME 下的体验更原生，无需额外配置。而 fcitx5 虽然功能更丰富，但在 GNOME 下需要额外处理与 ibus 的冲突（GNOME 登录时会同时启动两个输入法框架，造成混乱）。

三、niri 和 Hyprland 下配置中文输入法（fcitx5）3.1 为什么 niri&#x2F;Hyprland 用 fcitx5？ibus 与 GNOME 耦合较深，在 niri、Hyprland 等独立窗口管理器下问题较多：输入法候选框位置错误、GTK 应用无法输入等。fcitx5 对 Wayland 合成器的支持更好，是平铺式窗口管理器用户的首选。
fcitx5 支持的输入方案也更丰富：除了拼音，还有五笔、仓颉、Rime（支持各种方言输入）等。
3.2 安装 fcitx5# fcitx5 本体sudo pacman -S fcitx5# Rime 输入法引擎（包含拼音、五笔等多种方案）sudo pacman -S fcitx5-rime# fcitx5 图形配置工具sudo pacman -S fcitx5-configtool# GTK 和 Qt 输入法模块（让 GTK/Qt 应用能使用 fcitx5）sudo pacman -S fcitx5-gtk fcitx5-qt

为什么要安装 fcitx5-gtk 和 fcitx5-qt？
在 Wayland 下，输入法有两种通信方式：

Wayland text-input 协议（原生，效果最好）
输入法模块（GTK&#x2F;Qt 各自的模块，兼容性更广）

安装 fcitx5-gtk 和 fcitx5-qt 确保更多应用能正常调用输入法。
3.3 处理与 ibus 的冲突fcitx5 安装后，会在 /etc/xdg/autostart/ 下放置一个自启文件 fcitx5.desktop。如果你的系统同时安装了 GNOME（或者之前用过 GNOME），登录桌面时可能同时启动 ibus 和 fcitx5 两个输入法框架，造成冲突。
处理方法：删除 fcitx5 的 XDG autostart 文件
sudo rm /etc/xdg/autostart/fcitx5.desktop

删掉之后，fcitx5 不再通过 XDG 自动启动，改为由合成器（niri&#x2F;Hyprland）的配置文件显式启动。这样 GNOME 用 ibus，niri&#x2F;Hyprland 用 fcitx5，互不干扰。

⚠️ 注意：如果你只用 niri&#x2F;Hyprland，不用 GNOME，也建议这样操作，因为手动在合成器配置里启动 fcitx5 比 XDG autostart 更可控，可以精确控制启动时机。

3.4 配置环境变量输入法框架需要通过环境变量告知各个工具包（GTK、Qt）应该使用哪个输入法：
niri 配置（~/.config/niri/config.kdl）：
environment &#123;    GTK_IM_MODULE &quot;fcitx&quot;    QT_IM_MODULE &quot;fcitx&quot;    XMODIFIERS &quot;@im=fcitx&quot;    SDL_IM_MODULE &quot;fcitx&quot;&#125;spawn-at-startup &quot;fcitx5&quot; &quot;-d&quot;

解析：

GTK_IM_MODULE &quot;fcitx&quot;：告诉所有 GTK 应用（如 Firefox、Nautilus）使用 fcitx 输入法模块
QT_IM_MODULE &quot;fcitx&quot;：告诉所有 Qt 应用（如 JetBrains IDE、VLC）使用 fcitx 输入法模块
XMODIFIERS &quot;@im=fcitx&quot;：XIM 协议的输入法设置，用于老旧的 X11 应用（通过 XWayland 运行时）
SDL_IM_MODULE &quot;fcitx&quot;：告诉 SDL2 应用（如某些游戏）使用 fcitx
spawn-at-startup &quot;fcitx5&quot; &quot;-d&quot;：niri 启动时自动以后台模式（-d）运行 fcitx5

Hyprland 配置（新版 Lua 格式，~/.config/hypr/hyprland.lua）：
-- 设置输入法环境变量hl.env(&quot;GTK_IM_MODULE&quot;, &quot;fcitx&quot;)hl.env(&quot;QT_IM_MODULE&quot;, &quot;fcitx&quot;)hl.env(&quot;XMODIFIERS&quot;, &quot;@im=fcitx&quot;)hl.env(&quot;SDL_IM_MODULE&quot;, &quot;fcitx&quot;)-- 开机自启 fcitx5hl.on(&quot;hyprland.start&quot;, function()    hl.exec_cmd(&quot;fcitx5 -d&quot;)end)

Hyprland 配置（旧版 hyprlang 格式，~/.config/hypr/hyprland.conf）：
# 输入法环境变量env = GTK_IM_MODULE,fcitxenv = QT_IM_MODULE,fcitxenv = XMODIFIERS,@im=fcitxenv = SDL_IM_MODULE,fcitx# 开机自启 fcitx5（后台静默模式）exec-once = fcitx5 -d

3.5 配置 fcitx5-rime 拼音输入首次启动 fcitx5 后，打开配置工具：
fcitx5-configtool

在图形界面里：

点击左下角 + 号添加输入法
取消勾选”只显示当前语言”
搜索 “rime” 或 “中文”，添加 Rime
点击 OK 保存

Rime 默认包含”朙月拼音”（繁体）和”仓颉五代”。要使用简体拼音，有两种方式：
方式 A：切换 Rime 内置方案
fcitx5-rime 安装后，默认会包含简体拼音。在输入法激活状态下，按 F4 或 Ctrl+~ 可以打开方案选择菜单，选择”朙月拼音·简化字”。
方式 B：安装 rime-pinyin-zhwiki 词库（可选，扩充词汇量）
yay -S rime-pinyin-zhwiki

安装后在 fcitx5-configtool 里点击”重新部署 Rime”即可。
3.6 切换输入法fcitx5 默认用 Ctrl + Space 切换中英文，用 Ctrl + Shift 切换不同输入法。可以在 fcitx5-configtool 的”全局选项”里修改。
3.7 验证输入法是否工作打开 kitty 终端，切换到中文输入法，输入拼音，应该能看到候选词列表浮现。
如果没有出现候选框，执行诊断：
fcitx5-diagnose

这个命令会详细检查 fcitx5 的运行状态和环境变量配置，并给出修复建议。

四、中文字体渲染优化（可选）4.1 配置字体优先级在某些情况下，系统可能会选择日文版本的汉字字体，导致字形看起来”怪怪的”（日文汉字和中文简体字形有细微差异）。通过字体配置文件指定优先使用中文字体：
mkdir -p ~/.config/fontconfignano ~/.config/fontconfig/fonts.conf

写入：
&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE fontconfig SYSTEM &quot;fonts.dtd&quot;&gt;&lt;fontconfig&gt;  &lt;!-- 优先使用中文字体渲染中文 --&gt;  &lt;match&gt;    &lt;test name=&quot;lang&quot; compare=&quot;contains&quot;&gt;      &lt;string&gt;zh&lt;/string&gt;    &lt;/test&gt;    &lt;edit name=&quot;family&quot; mode=&quot;prepend&quot;&gt;      &lt;string&gt;Noto Sans CJK SC&lt;/string&gt;    &lt;/edit&gt;  &lt;/match&gt;    &lt;!-- 优先使用中文字体渲染日文 Unicode 区的汉字 --&gt;  &lt;alias&gt;    &lt;family&gt;serif&lt;/family&gt;    &lt;prefer&gt;      &lt;family&gt;Noto Serif CJK SC&lt;/family&gt;    &lt;/prefer&gt;  &lt;/alias&gt;  &lt;alias&gt;    &lt;family&gt;sans-serif&lt;/family&gt;    &lt;prefer&gt;      &lt;family&gt;Noto Sans CJK SC&lt;/family&gt;    &lt;/prefer&gt;  &lt;/alias&gt;  &lt;alias&gt;    &lt;family&gt;monospace&lt;/family&gt;    &lt;prefer&gt;      &lt;family&gt;Noto Sans Mono CJK SC&lt;/family&gt;    &lt;/prefer&gt;  &lt;/alias&gt;&lt;/fontconfig&gt;

刷新字体缓存：
fc-cache -fv


五、常见问题排查问题：终端里中文显示为乱码原因：终端的字体不包含中文字符，或者编码设置有问题。
解决：
# 确认当前编码是 UTF-8echo $LANG# 在 kitty 的配置里设置中文字体回退echo &#x27;font_family      JetBrains Monofont_size 13.0# 中文回退字体symbol_map U+4E00-U+9FFF,U+3400-U+4DBF Noto Sans CJK SC&#x27; &gt;&gt; ~/.config/kitty/kitty.conf

问题：fcitx5 启动了但无法输入中文原因：环境变量没有被程序读取到（程序在 fcitx5 设置环境变量之前启动）。
解决：

确认 niri&#x2F;Hyprland 配置文件里的 GTK_IM_MODULE 等变量已正确设置
关闭并重新打开应用
运行 fcitx5-diagnose 查看详细信息

问题：GNOME 下同时运行了 ibus 和 fcitx5原因：忘记删除 /etc/xdg/autostart/fcitx5.desktop。
解决：
sudo rm /etc/xdg/autostart/fcitx5.desktop

然后注销重新登录。
问题：JetBrains IDE 无法使用 fcitx5 输入中文原因：JetBrains IDE 默认运行在 XWayland 下，需要 XIM 支持。
解决：在 IDE 的 JVM 选项里添加：
-Drecreate.x11.input.method=true

在 IDE 里通过 Help → Edit Custom VM Options 添加。

小结至此，五篇博客的全部内容已经完成：



篇
内容



第一篇
通过 archinstall 安装 Arch Linux + 多种桌面环境


第二篇
通过 WSL 在 Windows 上安装 Arch Linux


第三篇
安装 git、yay、sdkman、nvm、uv、Go、Docker、LazyVim


第四篇
通过 DMS 配置 niri 和 Hyprland


第五篇
中文 locale、字体、ibus（GNOME）、fcitx5（niri&#x2F;Hyprland）


通过这五篇的配置，你拥有了一个：

完整的中文支持（界面 + 输入）
现代化的开发环境（Java&#x2F;Node&#x2F;Python&#x2F;Go&#x2F;Docker）
高效的编辑器（Neovim + LazyVim）
漂亮可用的桌面（DMS + niri 或 Hyprland）

的 Arch Linux 系统。
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>中文环境</tag>
        <tag>fcitx5</tag>
        <tag>ibus</tag>
        <tag>输入法</tag>
        <tag>locale</tag>
      </tags>
  </entry>
</search>
